"""Version constants for the OCR runtime.

`PROTOCOL_VERSION` is bumped only when the JSON-RPC wire contract changes in a
way the Node sidecar must notice; `RUNTIME_VERSION` tracks this package.
"""

RUNTIME_VERSION = "1.0.0-alpha.0"
PROTOCOL_VERSION = "1"
