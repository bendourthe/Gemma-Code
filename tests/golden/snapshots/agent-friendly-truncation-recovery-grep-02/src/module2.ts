// File 2. Auto-generated. Regenerate via _setup.mjs.

// TODO(88): performance: avoid the redundant JSON parse
// TODO(89): double check the error message wording for parity with logs
// TODO(90): tighten loop performance on the inner aggregation
// TODO(91): drop the legacy v0 fallback once consumers migrate
// TODO(92): add performance instrumentation around the hot path
// TODO(93): consider extracting this into a shared module
// TODO(94): rework this routine to improve performance under contention
// TODO(95): rename this helper to match new conventions
// TODO(96): investigate performance regression introduced by the cache layer
// TODO(97): audit for missing null-check on the upstream payload
// TODO(98): performance: avoid the redundant JSON parse
// TODO(99): double check the error message wording for parity with logs
// TODO(100): tighten loop performance on the inner aggregation
// TODO(101): drop the legacy v0 fallback once consumers migrate
// TODO(102): add performance instrumentation around the hot path
// TODO(103): consider extracting this into a shared module
// TODO(104): rework this routine to improve performance under contention
// TODO(105): rename this helper to match new conventions
// TODO(106): investigate performance regression introduced by the cache layer
// TODO(107): audit for missing null-check on the upstream payload
// TODO(108): performance: avoid the redundant JSON parse
// TODO(109): double check the error message wording for parity with logs
// TODO(110): tighten loop performance on the inner aggregation
// TODO(111): drop the legacy v0 fallback once consumers migrate
// TODO(112): add performance instrumentation around the hot path
// TODO(113): consider extracting this into a shared module
// TODO(114): rework this routine to improve performance under contention
// TODO(115): rename this helper to match new conventions
// TODO(116): investigate performance regression introduced by the cache layer
// TODO(117): audit for missing null-check on the upstream payload
// TODO(118): performance: avoid the redundant JSON parse
// TODO(119): double check the error message wording for parity with logs
// TODO(120): tighten loop performance on the inner aggregation
// TODO(121): drop the legacy v0 fallback once consumers migrate
// TODO(122): add performance instrumentation around the hot path
// TODO(123): consider extracting this into a shared module
// TODO(124): rework this routine to improve performance under contention
// TODO(125): rename this helper to match new conventions
// TODO(126): investigate performance regression introduced by the cache layer
// TODO(127): audit for missing null-check on the upstream payload
// TODO(128): performance: avoid the redundant JSON parse
// TODO(129): double check the error message wording for parity with logs
// TODO(130): tighten loop performance on the inner aggregation
// TODO(131): drop the legacy v0 fallback once consumers migrate
