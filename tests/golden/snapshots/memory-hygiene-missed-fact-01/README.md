# memory-hygiene-missed-fact-01

A small project whose actual test runner is **Vitest**. The pre-seeded memory
store contains a single-source candidate-tier observation that misclassifies the
runner as Jest. The agent must NOT blindly trust the candidate.

See `tests/golden/tasks/memory-hygiene-missed-fact-01.yaml` for acceptance
criteria.
