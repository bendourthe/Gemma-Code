# Plan -- Gemma-Code v0.8.0 Cycle (Multi-Source Adoption)

**Project**: Gemma-Code
**Version**: v0.8.0
**Slug**: v0.8.0-cycle
**Plan Type**: Feature/Enhancement (adoption-driven multi-theme)
**Created**: 2026-05-14
**Goal**: Adopt all in-scope items from [docs/v0.7.0/comparison-multi-source-v2.md](../../v0.7.0/comparison-multi-source-v2.md) (P0+P1+P2+P3) plus carry forward all open items from `docs/v0.7.0/known-gaps.md` Section 10, while preserving Gemma-Code's local-only thesis, sub-200 ms hot-path performance, and zero-network-egress posture.

Phase sequencing follows the MCP Registry Policy decision tree (reverse-engineer-first). See Section 9.4 of the source comparison for the ordering rationale: `skill-native` items ship first (Phase 1), then `re-full` / `re-partial` items in dependency order (Phases 2-6), then release polish (Phase 7). Eleven `drop-outright` items are documented in the appendix and not adopted.

## Overview

v0.8.0 is the **wrappers-and-artifacts** cycle. The comparison against seven external sources (hermes-agent, jola.dev M4, learn-harness-engineering, antirez/ds4, awesome-hermes-agent, omlx, plannotator) surfaced ~38 adoption candidates. Gemma-Code already has the *primitives* in place (4-layer memory, 6-stage compaction, hardware tiers, OTLP, mutation testing, Plan mode, MCP, sub-agents). v0.8.0 turns those primitives into a productized experience by adding the discrete harness artifacts (`feature_list.json`, `init.sh`, `clean-state-checklist.md`, `evaluator-rubric.md`), the prefix-cache-preserving memory snapshot semantics, the plannotator-grade plan-mode review UX, a prompt-injection scanner at memory boundaries, a hybrid memory scoring stack, a second LM Studio inference backend on Apple Silicon, a single `--trace` bug-report primitive, and a dual-loop skill curator with dry-run + rollback safety.

This plan ingests **10 open items carried forward from v0.7.0 known-gaps**: see sub-tasks tagged `[from v0.7.0 known-gaps: 10.O.N]`. Three (10.O.1, 10.O.2, 10.O.3 -- all P1 Phase-4 panel-host wiring carryovers) close in Phase 0; the remaining seven (P2/P3) land in their natural target phases (5.10, 5.11, 6.A, 7.1, 7.A, 7.B, 7.C).

The local-only thesis is unchanged. No outbound calls are added. The LM Studio second backend (Phase 4) is local-only and opt-in; the omlx third backend is **explicitly deferred to v0.9.0** due to alpha-stage maintenance risk. Eleven items are explicitly NOT adopted for security/policy reasons (Items N1-N11 in the appendix).

## Phases at a Glance

| Phase | Title | Outcome |
|-------|-------|---------|
| 0 | Cycle kickoff + v0.7.0 carryovers | Plan accepted; v0.7.0 baselines captured; three P1 panel-wiring carryovers closed; v0.6.0 exit verification passes |
| 1 | Skill-native quick wins (prompt-only) | 7 zero-code skills ship: compaction prefix, plan denial, PFM reminder, approved-with-notes, lens, incident-commander, council |
| 2 | Harness artifacts + memory snapshot + injection defense | `feature_list.json`, `init.sh`, `clean-state-checklist.md`, pass-state gating, frozen memory snapshot, streaming scrubber FSM, prompt-injection scanner, SKILL.md YAML standard |
| 3 | Plan-mode UX overhaul | Three annotation primitives, plan version archive + diff, quick-label chips, improvement-hook file |
| 4 | Observability + runtime + hybrid scoring | `/trace` primitive, LM Studio LLMClient, Gemma 4 channel parser, sampler presets + thinking modes, hybrid RRF scoring, why-retrieved, evaluator rubric template |
| 5 | Skill ecosystem maturation | Per-skill metrics, dual-loop curator with rollback, AST-scanned tool registry, `.gemma.md` git-root walk, shell-hook protocol, pre-tool compressor, single test runner, prompt linter |
| 6 | P2 backlog | Three-state sync return, intuition cache, reflect job, skill auto-harvest, architecture linter, TTL + pinning UI, M-series benchmarks, exact-bytes tool replay, streaming-aware tool emission |
| 7 | Polish, golden re-capture, security review, release | v0.7.0 + v0.6.0 + v0.4.0 baseline regeneration, full pen-test re-run, ADR-0007/0014 cross-ref fix, ship v0.8.0 |

---

## Phase 0: Cycle kickoff + v0.7.0 carryovers

**Goal**: Formally accept the plan, close v0.6.0 operator-action items and the three P1 panel-wiring carryovers from v0.7.0 Phase 4, capture v0.7.0 golden + benchmark baselines, and initialize the v0.8.0 known-gaps tracker.

**Prerequisites**: v0.7.0 tagged on origin/main; Ollama + gemma4:e4b available for live capture.

**Stability Gate**: `npm ci && npm run lint && npm run build && npm run test && npm run test:integration && npm run bench && npm run deps:check && npm run catalog:check && npm run perm-tier:check && npm audit --production --audit-level=moderate` all pass with zero errors/warnings; v0.7.0 + v0.6.0 + v0.4.0 golden baselines exist under `tests/golden/baselines/`; three P1 carryovers from `docs/v0.7.0/known-gaps.md` Section 10 are closed.

### Sub-tasks

#### 0.1 -- Accept the v0.8.0 cycle plan and create `known-gaps.md` shell

**Objective**: Stage and commit this plan file; create the empty `docs/v0.8.0/known-gaps.md` skeleton for the in-cycle gap log that Phases 1-7 will append to.

**Prompt**:
> Read [docs/v0.8.0/plans/v0.8.0-cycle.md](docs/v0.8.0/plans/v0.8.0-cycle.md) and the source comparison at [docs/v0.7.0/comparison-multi-source-v2.md](docs/v0.7.0/comparison-multi-source-v2.md). Stage both as the first v0.8.0 commit using the message `feat(v0.8.0): cycle plan kickoff (multi-source adoption)`. Then create `docs/v0.8.0/known-gaps.md` with the same skeleton structure as `docs/v0.7.0/known-gaps.md` (Status: `in-progress`; Sections 1-9 placeholders; Section 10 `## v0.8.0 in-cycle gap log` with empty Open Items / Resolved / Summary tables). Update `docs/v0.7.0/known-gaps.md` Section 10.1 to mark items 10.O.1, 10.O.2, 10.O.3 as `transferred to v0.8.0 plan (Phase 0)`, items 10.O.5 as `transferred to v0.8.0 plan (Phase 5)`, and items 10.O.4 + 10.O.6 as `transferred to v0.8.0 plan (Phase 7)`. Recompute the Section 10.3 Summary counts. Run `npm run lint && npm run build && npm run test` to confirm baseline. No code changes in this sub-task -- just docs.

#### 0.2 -- Live-Ollama golden + benchmark baseline capture (v0.4.0, v0.6.0, v0.7.0)

**Objective**: Close v0.6.0 operator-action item 1.1 and v0.7.0 DoD criterion 3.1 by capturing the three missing baselines.

**Prompt**:
> Read `docs/v0.6.0/development/history/phase-08.md` Section 3.1 for the full procedure. Execute the three captures:
> (1) `tests/benchmarks/baselines/v0.7.0.json` via `npm run bench -- --update-baseline`;
> (2) `tests/golden/baselines/v0.7.0.json` via `python tests/golden/framework/run_all.py --model gemma4:e4b --output tests/golden/baselines/v0.7.0.json` against live Ollama;
> (3) `tests/golden/baselines/v0.4.0.json` via `git worktree add ../Gemma-Code-v0.4.0 v0.4.0`, copy the current framework into the worktree (apples-to-apples), `npm ci`, and run the suite there.
> If `tests/golden/baselines/v0.6.0.json` does not exist either, capture it with the same procedure as (2) but on the v0.6.0 tag worktree.
> After all captures, run `scripts/check-bench-regressions.mjs` against v0.5.0, v0.6.0, and v0.4.0 baselines and document the deltas in `docs/v0.8.0/known-gaps.md` Section 1. Update the CHANGELOG v0.5.0 retrospective note block with the measured token-savings number against the v0.4.0 long-arc compare. Requires `ollama serve` running with `gemma4:e4b` pulled.

#### 0.3 -- [from v0.7.0 known-gaps: 10.O.1] Wire queued-message-field swap in panel host

**Objective**: Close v0.7.0 in-cycle gap 10.O.1 (P1): panel host swaps the input row for the queued-message field during streaming.

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10.1 row 10.O.1. The render primitive [src/panels/webview/render/queuedMessageField.ts](src/panels/webview/render/queuedMessageField.ts) ships. The swap is intentionally deferred. Wire `renderQueuedMessageField` toggle in [src/panels/ChatWebviewHost.ts](src/panels/ChatWebviewHost.ts) from the streaming start / end events emitted by [src/chat/StreamingPipeline.ts](src/chat/StreamingPipeline.ts). On streaming start, replace the input row with the queued-message field; on streaming end (or cancel), restore the input row. Add a unit test under `tests/unit/panels/ChatWebviewHost.test.ts` (jsdom + new Function harness pattern) asserting the swap happens in both directions. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` Section 10.2 noting "Resolved in v0.8.0 Phase 0.3".

#### 0.4 -- [from v0.7.0 known-gaps: 10.O.2] Route permissionPromptResponse to ConfirmationGate

**Objective**: Close v0.7.0 in-cycle gap 10.O.2 (P1): `ChatMessageRouter` routes `permissionPromptResponse` to `ConfirmationGate.resolvePrompt`.

**Prompt**:
> Read [src/panels/ChatMessageRouter.ts](src/panels/ChatMessageRouter.ts) and [src/tools/ConfirmationGate.ts](src/tools/ConfirmationGate.ts). Add a `permissionPromptResponse` case to the message router that calls `ConfirmationGate.resolvePrompt(promptId, decision, peer)` where `decision` is `allow` / `deny` and `peer` defaults to `local`. Verify the existing `messages.ts` protocol carries `promptId`, `decision`, and optional `peer` fields. Add an integration test under `tests/integration/panels/permissionPrompt.test.ts` that posts a fake `permissionPromptResponse`, runs the routing, and asserts the gate resolves correctly. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 0.4".

#### 0.5 -- [from v0.7.0 known-gaps: 10.O.3] Wire `ToolRegistryBuilder.todos` into panel bootstrap

**Objective**: Close v0.7.0 in-cycle gap 10.O.3 (P1): `ToolRegistryBuilder.todos` toggled for the active session inside panel bootstrap.

**Prompt**:
> Read [src/panels/ChatPanelBootstrap.ts](src/panels/ChatPanelBootstrap.ts) and [src/tools/ToolRegistryBuilder.ts](src/tools/ToolRegistryBuilder.ts). Toggle `todos: true` for the active session in `ChatPanelBootstrap`'s registry construction call. Verify the existing `todos` tool handler at [src/tools/handlers/todos.ts](src/tools/handlers/todos.ts) is registered and activated. Add a regression test under `tests/unit/panels/ChatPanelBootstrap.test.ts` asserting `todos` is included in the active tool set after bootstrap. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 0.5".

#### 0.6 -- Post-tag exit verification for v0.7.0

**Objective**: Close v0.6.0 operator-action item 1.2 (carried into v0.7.0) -- run the full gate against the v0.7.0 tag.

**Prompt**:
> Check out the v0.7.0 tag clean in a fresh worktree: `git worktree add ../Gemma-Code-v0.7.0-verify v0.7.0`. Run the full gate: `npm ci && npm run lint && npm run build && npm run test && npm run test:integration && npm run bench && npm run deps:check && npm run catalog:check && npm run perm-tier:check && npm audit --production --audit-level=moderate`. All must pass with zero errors and zero warnings. Verify the GitHub release artifact for v0.7.0 contains the VSIX. Re-run pen-test Attack Path A simulation against the v0.7.0 source (see `docs/v0.6.0/review/penetration-test.md`). Document the result in `docs/v0.8.0/known-gaps.md` Section 1 and remove the worktree.

#### 0.7 -- Testing and stabilization (Phase 0)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `npm run lint && npm run build && npm run test && npm run test:integration` and confirm zero failures. Confirm that sub-tasks 0.3/0.4/0.5 each added a regression test that exercises the wiring. Update `docs/v0.8.0/known-gaps.md` Section 10 with any new in-cycle gaps surfaced during Phase 0. Run `/generate-session-history` to document Phase 0 at `docs/v0.8.0/development/history/phase-00.md`.

### Phase 0 Exit Checklist

- [ ] Cycle plan accepted and committed
- [ ] v0.7.0 + v0.6.0 + v0.4.0 golden baselines captured
- [ ] v0.7.0 + v0.6.0 benchmark baselines regenerated
- [ ] Three P1 carryovers (10.O.1, 10.O.2, 10.O.3) closed with regression tests
- [ ] v0.7.0 post-tag exit verification passes
- [ ] `docs/v0.8.0/known-gaps.md` initialized
- [ ] Phase 0 session history at `docs/v0.8.0/development/history/phase-00.md`

---

## Phase 1: Skill-native quick wins (prompt-only)

**Goal**: Ship 7 zero-code skills that close capability gaps with prompt changes only. Per MCP Registry Policy, these are the highest-leverage adoptions because they ship immediately without any code risk.

**Prerequisites**: Phase 0 complete.

**Stability Gate**: All 7 new prompts compile into the system-prompt budget without overflow; existing `tests/unit/chat/PromptBuilder.test.ts` token-budget assertions still pass; new skills appear in `/help` output.

### Sub-tasks

#### 1.1 -- Compaction `SUMMARY_PREFIX` framing (item A3)

**Objective**: Adopt hermes-agent's structured compaction summary prefix to prevent the model from re-answering compacted questions.

**Prompt**:
> Read the comparison report Section 5a item A3 and the source quote from `agent/context_compressor.py:SUMMARY_PREFIX`. Create [src/chat/prompts/compaction.md](src/chat/prompts/compaction.md) with the prefix text: "The following is a compacted summary of the prior conversation, provided as BACKGROUND REFERENCE ONLY -- NOT as active instructions to act on. The authoritative current state lives in `Memory.md` / `Context.md`. Resume from the `## Active Task` section below." Edit [src/chat/CompactionStrategy.ts](src/chat/CompactionStrategy.ts) (or the LLM-summary strategy) to prepend this text to every compaction output. Add a unit test under `tests/unit/chat/CompactionStrategy.test.ts` asserting the prefix is present in the rendered summary. Token cost: ~70 tokens per compaction; verify the budget is not exceeded.

#### 1.2 -- Strong-framed plan-mode denial template (item B2)

**Objective**: Replace soft denial phrasing with the plannotator-tuned strong-directive template.

**Prompt**:
> Read the comparison report Section 5a item B2 and the source quote from `packages/shared/prompts.ts:41-42`. Edit [src/chat/PlanMode.ts](src/chat/PlanMode.ts) to add a `denialTemplate` constant: "YOUR PLAN WAS NOT APPROVED.\n\nYou MUST revise the plan to address ALL of the feedback below before re-submitting.\n\nRules:\n- Do NOT resubmit the same plan unchanged.\n- Do NOT change the plan title (first `#` heading) unless the user explicitly asks you to.\n\nUSER FEEDBACK:\n{{feedback}}". When `denyStep()` or a future `denyPlan()` path is invoked, append this template with the user's feedback into the conversation as a system message. Add a unit test asserting the template is emitted on deny.

#### 1.3 -- PFM reminder injection on plan-mode entry (item B3)

**Objective**: Inject capability hints about the v0.7.0 Phase 4 render protocol primitives so the agent uses them in its plans.

**Prompt**:
> Read the comparison report Section 5a item B3 and the source `packages/shared/pfm-reminder.ts`. Create [src/chat/prompts/planModeCapabilities.md](src/chat/prompts/planModeCapabilities.md) listing the 7 render primitives shipped in v0.7.0 Phase 4 (DIFF_CARD, ACTION_TAG, PERMISSION_PROMPT, TODO_BLOCK, THOUGHT_META_ROW, QUEUED_MESSAGE_FIELD, COMPLETION_REPORT) and describe how Gemma can reference them in plan output (e.g., emit a numbered plan with `- [ ]` task checkboxes that the webview renders as TODO_BLOCK). Edit `PlanMode.toggle()` to append this capabilities note when plan mode activates. Validate token cost via `PromptBudget` and confirm it stays within the system-prompt budget.

#### 1.4 -- Approved-with-notes plan-mode path (item B4)

**Objective**: Let the user approve a plan while attaching implementation notes for the executor.

**Prompt**:
> Read the comparison report Section 5a item B4 and source `packages/shared/prompts.ts:47-48`. Edit [src/chat/PlanMode.ts](src/chat/PlanMode.ts) to add an `approveWithNotes(notes: string)` method. When invoked, transition all pending steps to `approved` (same as full approve) and inject a system message: "Plan approved with notes! Execute the plan. ## Implementation Notes\n\nThe user approved your plan but added the following notes to consider during implementation:\n\n{{notes}}\n\nProceed with implementation, incorporating these notes where applicable." Add the corresponding UI handler -- a textarea in the plan-mode webview footer that submits via a new `messages.ts` shape `planApproveWithNotes`. Add a unit test.

#### 1.5 -- Lens generation skill (item D8)

**Objective**: Add a `/lens` skill that asks the agent to write its own analytical lens before answering, raising Gemma 4's reasoning ceiling.

**Prompt**:
> Read the comparison report Section 5a item D8 (super-hermes). Create [src/skills/catalog/lens/SKILL.md](src/skills/catalog/lens/SKILL.md) with YAML frontmatter `name: lens / description: "Generate an analytical lens for a complex problem before answering" / version: 1.0.0 / platforms: [linux, macos, windows] / metadata.hermes.tags: [reasoning, meta-prompting]`. Body: a 3-step prompt that asks the agent to (1) restate the user's question in 1 sentence, (2) write a 3-5 bullet "lens" of the angles a senior engineer would consider, (3) answer the question through that lens. Register the skill in [src/skills/SkillLoader.ts](src/skills/SkillLoader.ts) and verify `/help` lists it.

#### 1.6 -- Incident-commander skill (item E6)

**Objective**: Add a `/incident-commander` skill that runs triage -> diagnose -> remediate -> verify in safety-tiered order.

**Prompt**:
> Read the comparison report Section 5a item E6 (hermes-incident-commander). Create [src/skills/catalog/incident-commander/SKILL.md](src/skills/catalog/incident-commander/SKILL.md) with YAML frontmatter as in 1.5. Body: a structured prompt for triaging test failures or build breaks. Steps: (1) read failing output, (2) classify failure (regression / flake / env / dependency), (3) write a 3-tier remediation list ordered by safety (read-only diagnosis first, then no-op verifications, then minimal targeted fix), (4) execute lowest-tier fix only, (5) re-run verification, (6) write a post-incident playbook entry to `docs/v0.8.0/development/incidents/<date>-<slug>.md`. Register in `SkillLoader`.

#### 1.7 -- Council skill (item G7)

**Objective**: Add a `/council` skill that runs 3 adversarial perspectives on a major decision before committing.

**Prompt**:
> Read the comparison report Section 5a item G7 (hermes-council). Create [src/skills/catalog/council/SKILL.md](src/skills/catalog/council/SKILL.md) with YAML frontmatter as in 1.5. Body: a structured 3-pass prompt that has the agent argue (1) for the proposed change, (2) against it as a senior architect, (3) from the user-impact perspective. Final synthesis: list 1-3 acceptance criteria and 1-3 explicit risks. Register in `SkillLoader`. This skill is intentionally heavy (3 inference passes); document the latency cost in the SKILL.md body.

#### 1.8 -- Testing and stabilization (Phase 1)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `npm run lint && npm run build && npm run test`. Confirm zero failures. Validate token-budget impact: `PromptBudget` should still leave >=20% margin in the system-prompt section after the new prompts. Manually exercise `/lens`, `/incident-commander`, and `/council` against a small Gemma 4 model via live Ollama and confirm output quality. Run `/generate-session-history` to document Phase 1 at `docs/v0.8.0/development/history/phase-01.md`.

### Phase 1 Exit Checklist

- [ ] 7 sub-tasks completed
- [ ] All tests passing
- [ ] System-prompt budget still within limits
- [ ] `/help` lists 3 new skills (`lens`, `incident-commander`, `council`)
- [ ] Manual smoke test of new skills against live Ollama
- [ ] Phase 1 session history written

---

## Phase 2: Harness artifacts + memory snapshot + injection defense

**Goal**: Ship the discrete artifact set (`feature_list.json`, `init.sh`, `clean-state-checklist.md`), introduce pass-state gating in `AgentLoop`, freeze the memory snapshot at session start, add the streaming memory-context scrubber FSM, install the prompt-injection scanner at memory write and context-file read boundaries, and migrate skills to the SKILL.md YAML frontmatter standard.

**Prerequisites**: Phase 1 complete.

**Stability Gate**: `npm run lint` zero; `npm run build` zero; `npm run test` 100% pass; `init.sh` runs to completion on Windows + macOS + Linux; `feature_list.json` validates against schema; `clean-state-checklist.md` 30 items execute; pass-state gate in `AgentLoop` blocks a task from claiming `done` without a verification tool call logged in `OperationLog`; `MemoryStore.writeEntry()` rejects entries containing prompt-injection patterns; all 14 existing skills migrated to YAML frontmatter format with backward-compat read path.

### Sub-tasks

#### 2.1 -- `feature_list.json` as versioned scope contract (item C1)

**Objective**: Create the machine-readable feature contract and wire it into the golden task suite.

**Prompt**:
> Read the comparison report Section 5a item C1 and the source `projects/project-06/solution/feature_list.json`. Create `feature_list.json` at the repo root with the shape `{ "version": "v0.8.0", "features": [{ "id": "f001", "name": "...", "description": "...", "status": "not_started|active|blocked|passing", "evidence": "<path or command>", "testedAt": "<ISO date or null>", "verificationCommand": "<shell command>" }, ...] }`. Seed it with one feature per Phase 2-6 sub-task in this plan, plus the foundational features (memory layers, compaction pipeline, plan mode, MCP) already in the codebase. Create [src/evaluation/FeatureList.ts](src/evaluation/FeatureList.ts) with `loadFeatureList()`, `markPassing(id, evidence)`, and `validate()`. Extend [src/evaluation/GoldenTaskSuite.ts](src/evaluation/GoldenTaskSuite.ts) so that when a golden task passes, the matching feature row flips to `passing` and stamps `testedAt`. Document the format in `docs/v0.8.0/feature-list-format.md`.

#### 2.2 -- `init.sh` + `init.ps1` lifecycle bootstrap (item C2)

**Objective**: Add a verified 5-step bootstrap that confirms deps + typecheck + build + harness files + sample data before any work begins.

**Prompt**:
> Read the comparison report Section 5a item C2 and the source `projects/project-06/solution/init.sh`. Create [scripts/init.sh](scripts/init.sh) (POSIX) and [scripts/init.ps1](scripts/init.ps1) (Windows PowerShell) that run: (1) `npm ci`, (2) `npm run lint`, (3) `npm run build`, (4) check that `AGENTS.md`, `ARCHITECTURE.md`, `feature_list.json`, `clean-state-checklist.md`, `docs/v0.8.0/plans/v0.8.0-cycle.md`, and `docs/v0.8.0/known-gaps.md` all exist, (5) check that `assets/specialists/{research,verification,planning,orchestration}.md` exist. Exit code 0 only if all steps pass; otherwise exit with a descriptive error. Reference both scripts in `AGENTS.md` "Startup Rules" section. Add a CI job `init-check` that runs `init.sh` on Linux + `init.ps1` on Windows.

#### 2.3 -- `clean-state-checklist.md` end-of-session gate + scanner (item C3)

**Objective**: Add the 30-item end-of-session gate plus a `cleanup-scanner.mjs` that detects orphan artifacts.

**Prompt**:
> Read the comparison report Section 5a item C3 and the source `projects/project-06/solution/{clean-state-checklist.md, scripts/cleanup-scanner.sh}`. Create [clean-state-checklist.md](clean-state-checklist.md) with 30 binary checks grouped into 7 categories: Build, Architecture, Runtime, Logging, Data, Performance, Repo. Each check has a checkbox and a verification command. Create [scripts/cleanup-scanner.mjs](scripts/cleanup-scanner.mjs) (Node, no deps) that scans for: orphan rows in `MemoryStore` (entries whose `sessionId` no longer exists in `ChatHistoryStore`), orphan FTS5 rows, dangling embeddings (zero-vector or NaN), references to deleted file paths in `Context.md`, and stale `.gemma-code/cache/*` files older than 30 days. Output JSON or human-readable mode (`--format=json|text`). Wire `cleanup-scanner.mjs` into the existing `/wrap-up-session` skill. Add an integration test under `tests/integration/storage/cleanupScanner.test.ts`.

#### 2.4 -- Pass-state gating in `AgentLoop` (item C8)

**Objective**: Prevent the agent from self-declaring task completion without an exit-0 verification tool call.

**Prompt**:
> Read the comparison report Section 5a item C8 and the source `docs/en/lectures/lecture-08/index.md` ("pass-state gating"). Edit [src/tools/AgentLoop.ts](src/tools/AgentLoop.ts): when the model emits a `done`-like terminal token (no further tool calls), require that at least one verification-class tool call (run_terminal returning exit 0, golden task pass, lint pass) has been logged via [src/observability/OperationLog.ts](src/observability/OperationLog.ts) since the last user message. If no verification log entry exists, inject a system message: "Task cannot complete without verification. Run a verification tool (lint, build, test, or relevant check) and re-emit the completion signal." Reset the loop counter. Configurable via a new setting `gemma-code.passStateGating` (default `true`). Add unit tests for: (a) task that runs `npm test` and succeeds flips to verified; (b) task that emits done without verification is rejected once; (c) setting can be disabled. Update `docs/adr/0015-pass-state-gating.md` (new ADR).

#### 2.5 -- Frozen memory snapshot at session start (item A1)

**Objective**: Inject `Memory.md` + `Context.md` content at session start; write-through to disk mid-session without re-rendering the prompt.

**Prompt**:
> Read the comparison report Section 5a item A1 and the source `tools/memory_tool.py` header. Create [src/storage/MemorySnapshot.ts](src/storage/MemorySnapshot.ts) with `captureAtSessionStart(workspaceId): MemorySnapshot` that loads `~/.gemma-code/memory/<workspace-id>/{Instructions.md, Memory.md, Context.md}` into an immutable object. Edit [src/chat/PromptBuilder.ts](src/chat/PromptBuilder.ts) to take the snapshot as input and freeze its content in the prompt for the lifetime of the session; mid-session writes go to disk via [src/storage/MemoryFiles.ts](src/storage/MemoryFiles.ts) but do NOT update the snapshot. Add a new setting `gemma-code.memorySnapshotMode = "frozen" | "live"` (default `frozen`); document the prefix-cache motivation. Update [docs/adr/0014-memory-file-architecture.md](docs/adr/0014-memory-file-architecture.md) Consequences section with the new semantics. Add unit tests covering: snapshot capture, frozen mode (mid-session write does not change rendered prompt), live mode (mid-session write does change prompt), prompt-budget invariance.

#### 2.6 -- Streaming memory-context scrubber FSM (item A2)

**Objective**: Strip `<memory-context>...</memory-context>` spans from streaming output across chunk boundaries.

**Prompt**:
> Read the comparison report Section 5a item A2 and the source `agent/memory_manager.py:StreamingContextScrubber`. Create [src/chat/MemoryContextScrubber.ts](src/chat/MemoryContextScrubber.ts) as a stateful FSM with three states: `outside`, `inside_tag`, `inside_span`. `feed(chunk: string): string` returns scrubbed content; `flush(): string` releases any held-back partial-tag tail. Wire into [src/chat/StreamingPipeline.ts](src/chat/StreamingPipeline.ts) at the chunk-emit boundary. Add unit tests that exercise: tag split across two chunks, nested but invalid tags, multiple tags in one chunk, EOF while inside a tag (must flush as-is).

#### 2.7 -- Prompt-injection scanner at memory + context boundaries (item G1)

**Objective**: Block prompt-injection patterns + invisible Unicode at MemoryStore writes and `Memory.md`/`Context.md` reads.

**Prompt**:
> Read the comparison report Section 5a item G1 and the sources `agent/prompt_builder.py:_scan_context_content` and `tools/memory_tool.py:_MEMORY_THREAT_PATTERNS`. Create [src/guardrails/PromptInjectionScanner.ts](src/guardrails/PromptInjectionScanner.ts) with `scan(text: string): {ok: boolean, findings: Finding[]}`. Pattern table: case-insensitive matches for `ignore previous instructions`, `disregard the above`, `you are now`, `<system>` outside our own delimiters, `eval(`, `process.exit`, base64-encoded blobs over 4 KB, and invisible-unicode codepoints (U+200B-U+200F, U+202A-U+202E, U+E0000-U+E007F). Block (throw) on any finding. Wire into [src/storage/MemoryStore.ts](src/storage/MemoryStore.ts)'s `writeEntry()` path AND into [src/storage/MemoryFiles.ts](src/storage/MemoryFiles.ts)'s read path with a fail-open mode (log + redact, do not crash) for legacy content. Add unit tests for every pattern. Update `docs/v0.6.0/review/penetration-test.md` with an Attack Path C entry covering this defense.

#### 2.8 -- SKILL.md YAML frontmatter migration (item D1)

**Objective**: Migrate the 14 existing skills to YAML frontmatter format aligned with agentskills.io standard.

**Prompt**:
> Read the comparison report Section 5a item D1 and the source `skills/dogfood/SKILL.md`. Edit [src/skills/SkillLoader.ts](src/skills/SkillLoader.ts) to support YAML frontmatter parsing (use `gray-matter` if not present; otherwise hand-roll a 50-line front-matter parser). Schema: `name` (required, kebab-case), `description` (required, 1 sentence), `version` (required, semver), `platforms` (optional, array of `linux|macos|windows`), `metadata.tags` (optional, array of strings), `metadata.related_skills` (optional, array of kebab-case names). Add a backward-compat path: if no frontmatter, parse the H1 as the name and the first paragraph as the description (v0.7.0 format) and log a deprecation warning. Migrate all 14 catalog skills under `src/skills/catalog/*/SKILL.md` to include frontmatter. Add 3 unit tests (frontmatter parse, fallback parse, deprecation warn). Update `docs/v0.1.0/tool-protocol.md` (or successor) with the new format.

#### 2.9 -- Testing and stabilization (Phase 2)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `npm run lint && npm run build && npm run test && npm run test:integration && npm run bench && npm run deps:check`. Confirm zero failures. Run `bash scripts/init.sh` (Linux/macOS) and `pwsh scripts/init.ps1` (Windows) and confirm exit 0. Run `node scripts/cleanup-scanner.mjs --format=json` against a development workspace and confirm a sane JSON report. Run the existing `tests/golden/framework/run_all.py` and confirm `feature_list.json` rows flipped to `passing` where appropriate. Update `docs/v0.8.0/known-gaps.md` Section 10 with any new in-cycle gaps. Run `/generate-session-history` to document Phase 2 at `docs/v0.8.0/development/history/phase-02.md`.

### Phase 2 Exit Checklist

- [ ] 8 sub-tasks completed
- [ ] `feature_list.json` validates and flips on golden-task pass
- [ ] `init.sh`/`init.ps1` exit 0 on all 3 platforms
- [ ] `clean-state-checklist.md` + scanner produce JSON report
- [ ] Pass-state gating active by default
- [ ] Memory snapshot frozen at session start
- [ ] Streaming scrubber strips memory-context spans
- [ ] Prompt-injection scanner blocks malicious writes
- [ ] All 14 skills migrated to YAML frontmatter
- [ ] Phase 2 session history written

---

## Phase 3: Plan-mode UX overhaul

**Goal**: Transform plan mode from "numbered list + step approval" into plannotator-grade structured review: three annotation primitives (DELETE/COMMENT/GLOBAL_COMMENT), plan version archive with diff renderer, quick-label chips, and a user-editable improvement-hook file.

**Prerequisites**: Phase 2 complete (depends on PFM reminder from Phase 1.3, plan-mode prompt updates from 1.2/1.4).

**Stability Gate**: User can annotate a plan via the three primitives in the webview; revising a plan shows a side-by-side diff against the prior version; quick-label chips inject the right `quickLabelTip` text; `~/.gemma-code/hooks/enterplanmode-improve.md` content is read on every plan-mode entry; existing plan-mode unit tests still pass.

### Sub-tasks

#### 3.1 -- Three annotation primitives (item B1)

**Objective**: Add DELETION / COMMENT / GLOBAL_COMMENT annotation rendering and message protocol.

**Prompt**:
> Read the comparison report Section 5a item B1 and the source `packages/ui/types.ts:1-54` (annotation type schema). Create [src/panels/webview/render/planAnnotation.ts](src/panels/webview/render/planAnnotation.ts) following the v0.7.0 Phase 4 render-primitive pattern (single-source `*_FN_SOURCE` string inlined into `RUNTIME_SCRIPT`). Schema: `Annotation = { id, blockId, startOffset, endOffset, type: "DELETION"|"COMMENT"|"GLOBAL_COMMENT", text?, originalText, quickLabelTip? }`. Render primitive: spans with strikethrough for DELETION, sidebar callout for COMMENT (anchored by data-attribute), top-of-plan callout for GLOBAL_COMMENT. Add three new `messages.ts` shapes: `planAnnotationAdd`, `planAnnotationRemove`, `planAnnotationsSubmit`. Wire through [src/panels/ChatMessageRouter.ts](src/panels/ChatMessageRouter.ts). Add unit tests under `tests/unit/panels/webview/render/planAnnotation.test.ts` using the jsdom + new Function harness.

#### 3.2 -- Plan version archive + 3-mode diff renderer (item A8 + B6)

**Objective**: Persist every plan revision under `~/.gemma-code/plans/<workspace>/<slug>/<N>.md` and render a side-by-side diff when the agent revises.

**Prompt**:
> Read the comparison report Section 5a items A8 and B6 and the sources `apps/hook/server/index.ts` + `packages/ui/hooks/usePlanDiff.ts`. Create [src/storage/PlanArchive.ts](src/storage/PlanArchive.ts) with `appendVersion(slug, content): number` (returns version index), `listVersions(slug): VersionEntry[]`, `getVersion(slug, n): string`, and `diff(slug, n1, n2): DiffResult` (3 modes: `clean` rendered word-diff, `classic` block diff, `raw` unified diff -- use `diff` npm package). Create [src/panels/webview/render/planDiff.ts](src/panels/webview/render/planDiff.ts) following the render-primitive pattern. On `PlanMode.setPlan(steps)`, call `PlanArchive.appendVersion(slug, plan)`. When the agent revises after denial, surface a `renderPlanDiff(slug, prev, curr)` message to the webview. Add unit tests + an integration test that exercises revise-then-diff.

#### 3.3 -- Quick-label chips (item B5)

**Objective**: Add reusable one-click annotation chips ("Out of scope", "Add test", "Risky", "Missing rationale", "Wrong file") with `quickLabelTip` auto-injection.

**Prompt**:
> Read the comparison report Section 5a item B5 and the source `packages/ui/components/QuickLabelDropdown.tsx`. Create [src/panels/webview/render/quickLabels.ts](src/panels/webview/render/quickLabels.ts) following the render-primitive pattern. Default chip set with their `quickLabelTip` text: "Out of scope" -> "This step extends beyond the agreed task boundary. Remove it or move to a follow-up plan.", "Add test" -> "Include test coverage for this step (unit + integration if behavior crosses module boundaries).", "Risky" -> "Flag the risk explicitly in the plan and include a verification step.", "Missing rationale" -> "Add a one-sentence rationale to this step explaining why this approach over alternatives.", "Wrong file" -> "Verify the file path before editing. Path appears to be incorrect for the stated change." Custom chips loadable from `~/.gemma-code/plans/quick-labels.json`. Wire into `planAnnotation.ts` so clicking a chip creates a COMMENT annotation prefilled with the tip text. Add unit tests.

#### 3.4 -- Improvement-hook file (item B7)

**Objective**: Read user-editable rules from `~/.gemma-code/hooks/enterplanmode-improve.md` and inject them on plan-mode entry.

**Prompt**:
> Read the comparison report Section 5a item B7 and the source `packages/shared/improvement-hooks.ts`. Create [src/chat/ImprovementHook.ts](src/chat/ImprovementHook.ts) with `loadHook(name: string): string | null` (reads `~/.gemma-code/hooks/<name>.md`, returns content or null). Edit `PlanMode.toggle()` to call `loadHook("enterplanmode-improve")` on activation and inject the content as a system message after the PFM reminder (Phase 1.3). Document the file format in `docs/v0.8.0/improvement-hooks.md` with examples (e.g., "When the plan touches the storage layer, always include a migration step. When the plan involves git operations, always include a backup checkpoint."). Add a CLI command `gemma-code.hooks.editPlanModeHook` that opens the file in VS Code. Add unit tests.

#### 3.5 -- Testing and stabilization (Phase 3)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `npm run lint && npm run build && npm run test && npm run test:integration`. Confirm zero failures. Manually exercise the plan-mode flow end-to-end: enter plan mode -> agent produces a plan -> annotate with all three primitives + a quick-label chip -> deny -> agent revises -> plan-diff renders -> approve-with-notes succeeds. Verify the improvement-hook content is injected by checking the rendered system prompt via the dev-tools `/debug-prompt` command (or equivalent). Run `/generate-session-history` to document Phase 3 at `docs/v0.8.0/development/history/phase-03.md`.

### Phase 3 Exit Checklist

- [ ] 4 sub-tasks completed
- [ ] Three annotation primitives render in the webview
- [ ] Plan archive at `~/.gemma-code/plans/` persists versions
- [ ] Plan-diff renderer shows clean/classic/raw modes
- [ ] Quick-label chips inject correct tip text
- [ ] Improvement-hook file content is injected on plan-mode entry
- [ ] Phase 3 session history written

---

## Phase 4: Observability + runtime + hybrid scoring

**Goal**: Ship a single `/trace` bug-report primitive, add LM Studio as a second `LLMClient` adapter with auto-detect on Apple Silicon, reverse-engineer omlx's Gemma 4 channel parser, add per-model sampler presets and three thinking modes, formalize prefix-aware system-prompt construction, and ship hybrid RRF memory scoring with why-retrieved transparency. Also ship the evaluator-rubric template and the handoff/progress doc split.

**Prerequisites**: Phase 3 complete.

**Stability Gate**: `/trace --enable` then `--dump` produces a single redacted file with all rendered prompts + tool calls + cache decisions; `LmStudioClient` auto-detected at `:1234` on macOS; Gemma 4 channel parser strips `<think>` blocks correctly on multi-turn replay; three thinking modes selectable via `/thinking-mode`; hybrid scoring returns RRF-fused results with `reason: string[]` on every entry; `docs/v0.8.0/review/evaluator-rubric.md` template renders correctly via `/run-deep-review`.

### Sub-tasks

#### 4.1 -- Single `--trace` bug-report file primitive (item G3)

**Objective**: Unify existing `Tracer` + `OperationLog` into one user-facing dump suitable for attaching to bug reports.

**Prompt**:
> Read the comparison report Section 5a item G3 and the source `ds4 README.md:739-748`. Create [src/observability/TraceFile.ts](src/observability/TraceFile.ts) with `enable(path?)`, `disable()`, `dump(path)`, `clear()`. While enabled, append one JSON line per event to the path: tool call (with redacted secret paths via the existing `secretPaths.ts` denylist), rendered system prompt, compaction event, cache decision, sub-agent spawn, plan transition. Implement `/trace` slash command in [src/commands/CommandRouter.ts](src/commands/CommandRouter.ts) with subcommands `enable [path]`, `dump [path]`, `clear`. Default path: `~/.gemma-code/trace/<session-id>.jsonl`. Wire `TraceFile` as a passive listener on the existing `Tracer` event bus. Add unit tests + redaction tests.

#### 4.2 -- LM Studio as second `LLMClient` adapter (item F1)

**Objective**: Add a second backend that speaks the OpenAI streaming format at `:1234`, with auto-detection.

**Prompt**:
> Read the comparison report Section 5a item F1 and the source jola.dev article. Create [src/llm/LmStudioClient.ts](src/llm/LmStudioClient.ts) implementing the existing `LLMClient` port from [src/llm/types.ts](src/llm/types.ts). Routes: `POST /v1/chat/completions` (SSE stream with `data: {...}` lines and `[DONE]` sentinel), `POST /v1/embeddings`. Adapt OpenAI message shape (`choices[0].delta.content`) to Gemma-Code's existing internal shape. Add settings: `gemma-code.llm.backend = "ollama" | "lmstudio" | "auto"` (default `auto`), `gemma-code.lmstudio.baseUrl` (default `http://127.0.0.1:1234`). Auto-detect: probe `:1234/v1/models` first on macOS, fall back to Ollama at `:11434`. On Windows/Linux, default to Ollama unless explicitly configured. Update [src/runtime/GemmaRuntime.ts](src/runtime/GemmaRuntime.ts) composition root. Add an ADR `docs/adr/0016-second-llm-backend.md`. Add unit tests + an integration test that exercises a live LM Studio server when available (skipped otherwise).

#### 4.3 -- Gemma 4 channel parser (item F3)

**Objective**: Reverse-engineer omlx's `omlx/adapter/gemma4.py` parser into TypeScript.

**Prompt**:
> Read the comparison report Section 5a item F3 and the source `omlx/adapter/gemma4.py`. Create [src/llm/Gemma4Parser.ts](src/llm/Gemma4Parser.ts) -- a pure parsing module, no runtime dependencies. Functions: `parseChannel(text): {visible: string, thought: string, toolResponse?: string}` handles `<|channel>thought\n...<channel|>`, `<turn|>`, `<|tool_response>...<tool_response|>`, `<start_function_call>`. Strip leading `<think>` blocks on multi-turn replay (used by `ConversationManager.replayForCompaction()`). Apache-2.0-clean rewrite (no copied lines). Wire into [src/chat/StreamingPipeline.ts](src/chat/StreamingPipeline.ts) just before the markdown renderer. Unit tests must include fixtures from real Gemma 4 outputs.

#### 4.4 -- Per-model sampler presets + thinking modes (items F4, F5, E4)

**Objective**: Extend per-model context schema with `tools` / `reasoning` / `max_tokens` / `thinkingFormat` and ship three thinking-mode presets (`nothink` / `think` / `think-max`).

**Prompt**:
> Read the comparison report Section 5a items F4, F5, E4 and the jola.dev article (sampler values: `temp 0.6 / top_p 0.95 / top_k 20 / min_p 0.0`). Extend [src/config/PerModelLimits.ts](src/config/PerModelLimits.ts) (or its v0.7.0 successor) with optional fields per model: `tools: boolean`, `reasoning: boolean`, `maxTokens: number`, `thinkingFormat: "gemma-chat-template" | "qwen-chat-template" | null`. Ship a sampler-preset table: `nothink` = `{temp: 0.7, top_p: 0.95, top_k: 64}` (default), `think` = `{temp: 0.6, top_p: 0.95, top_k: 20, min_p: 0.0}` (Qwen/jola tuned), `think-max` = same as `think` but with `reasoning: true` and `max_tokens: 32768`. Auto-downgrade `think-max` -> `think` when the configured context budget is below 64K. Implement `/thinking-mode <name>` slash command in `CommandRouter`. Add a settings toggle and a MemoryPanel UI control. Add unit tests.

#### 4.5 -- Prefix-aware system-prompt construction (item F7)

**Objective**: Lock prefix ordering in `PromptBuilder` so the prefix is identical across tool turns -- maximizes Ollama KV-cache hit and gives LM Studio's MLX path room to optimize.

**Prompt**:
> Read the comparison report Section 5a item F7 and source `omlx/cache/prefix_cache.py` (concept reference). Refactor [src/chat/PromptBuilder.ts](src/chat/PromptBuilder.ts) so that the system-prompt sections are built in a strict fixed order: (1) identity, (2) tool declarations (Phase 5 Sub-task 5.4 enforces these are stable per session), (3) frozen memory snapshot (from Phase 2.5), (4) skill index (loaded once at session start), (5) plan-mode capabilities (Phase 1.3, when active). Variable per-turn content (current user message, recent tool results) appended after the locked prefix. Add a property test that asserts the first N tokens (where N = locked-prefix length) are byte-identical across two adjacent tool turns of the same session. Update `docs/v0.2.0/architecture.md` and `docs/adr/0014-memory-file-architecture.md`.

#### 4.6 -- Hybrid RRF memory scoring + why-retrieved (items A5, A6)

**Objective**: Adopt Mnemosyne's hybrid scoring (vector + lexical + recency RRF or 50/30/20 weighted) and attach a `reason: string[]` explanation to every retrieved entry.

**Prompt**:
> Read the comparison report Section 5a items A5 and A6 and the Mnemosyne + yantrikdb references. Create [src/storage/HybridRanker.ts](src/storage/HybridRanker.ts) with `rank(query: string, candidates: MemoryEntry[]): RankedEntry[]` where `RankedEntry = MemoryEntry & {score: number, reason: string[]}`. Compute three sub-scores: lexical (BM25-style over FTS5 matches), vector (cosine over embeddings), recency (exponential decay from `updatedAt`). Fuse via reciprocal rank fusion (RRF, k=60) OR a 50/30/20 weighted sum (configurable via setting `gemma-code.memory.scoringMethod = "rrf" | "weighted"`, default `rrf`). Each `reason` entry is a one-sentence explanation (e.g., "Lexical match on 'authentication' rank #2", "Vector similarity 0.83 to query embedding", "Updated 3 minutes ago"). Wire `HybridRanker` into [src/storage/UnifiedMemoryRetriever.ts](src/storage/UnifiedMemoryRetriever.ts). Surface `reason` in the MemoryPanel UI as a collapsible "why retrieved" affordance. Unit tests + a property test that any RRF result includes at least one reason.

#### 4.7 -- Evaluator-rubric template + handoff/progress split (items C4, C5)

**Objective**: Ship the structured 1-5 rubric template and split end-of-session documentation into forward-looking `session-handoff.md` and chronological `claude-progress.md` (named `session-progress.md` for Gemma-Code).

**Prompt**:
> Read the comparison report Section 5a items C4 and C5 and the source `projects/project-06/solution/{evaluator-rubric.md, quality-document.md, session-handoff.md, claude-progress.md}`. Create [docs/v0.8.0/review/evaluator-rubric.md](docs/v0.8.0/review/evaluator-rubric.md) -- a template with 15 criteria across 5 categories (Correctness, Architecture, Verification, Documentation, Operability), each scored 1-5 with anchored descriptions. Create [docs/v0.8.0/review/quality-document.md](docs/v0.8.0/review/quality-document.md) template -- a separate A-F overall grade summary with evidence pointers. Edit the existing `/wrap-up-session` skill (and `/run-deep-review` if present) to: (a) emit a `session-handoff.md` (forward-looking: open issues, decisions pending, next session's starting point) and (b) emit a `session-progress.md` (chronological log of what happened this session). Add doc files to `docs/v0.8.0/development/<session-id>/`. Update the skill body to reference the rubric and quality-document templates. Add an integration test that exercises `/wrap-up-session` against a synthetic session and confirms both files exist.

#### 4.8 -- Testing and stabilization (Phase 4)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `npm run lint && npm run build && npm run test && npm run test:integration`. Confirm zero failures. Manually exercise: enable `/trace`, run a 3-turn session with a tool call, then `/trace dump`. Inspect the trace JSONL for redaction completeness. On macOS with LM Studio installed and running, confirm auto-detect picks LM Studio over Ollama. Run a Gemma 4 query through the LM Studio path and verify `Gemma4Parser` strips `<think>` blocks correctly. Toggle `/thinking-mode think` and confirm the sampler preset is applied. Run the hybrid scoring against a populated memory store and inspect `reason` arrays. Run `/wrap-up-session` and confirm both `session-handoff.md` and `session-progress.md` are written. Update `docs/v0.8.0/known-gaps.md`. Run `/generate-session-history` to document Phase 4 at `docs/v0.8.0/development/history/phase-04.md`.

### Phase 4 Exit Checklist

- [ ] 7 sub-tasks completed
- [ ] `/trace` produces a single redacted JSONL file
- [ ] LM Studio backend auto-detected on macOS
- [ ] Gemma 4 channel parser strips `<think>` correctly
- [ ] `/thinking-mode` switches sampler presets
- [ ] Prefix-aware prompt construction is byte-identical across tool turns
- [ ] Hybrid RRF scoring with `reason` arrays
- [ ] Evaluator rubric + handoff/progress templates render via `/wrap-up-session`
- [ ] Phase 4 session history written

---

## Phase 5: Skill ecosystem maturation

**Goal**: Per-skill success metrics, dual-loop curator with dry-run + rollback, AST-scanned tool registry, `.gemma.md` git-root walk discovery, shell-hook stdin-JSON/stdout-decision protocol, pre-tool command compressor, single test runner with sub-modes, and a prompt linter. Also closes v0.7.0 known-gap 10.O.5 (Promote-to-Memory section mapping) and 10.O.6 (Phase 5 documentation cross-link).

**Prerequisites**: Phase 4 complete.

**Stability Gate**: Per-skill success metrics emit via Tracer; `CurationLoop.dryRun()` produces a manifest without modifying state; `CurationLoop.apply(manifestId)` reads back the manifest and applies with rollback record; AST-scanned registry loads all 14 skills + 3 new ones; `.gemma.md` walk discovers files from cwd up to git root; existing 3 hooks accept the new stdin-JSON protocol; pre-tool compressor reduces a `cargo test`/`npm test`/`git diff` output by >=60%; `scripts/test.mjs --mode=unit|integration|golden|bench|mutation` works.

### Sub-tasks

#### 5.1 -- Per-skill success metrics (item D5)

**Objective**: Track invocation count, success rate, retry-loop count, and user-correction count per skill.

**Prompt**:
> Read the comparison report Section 5a item D5 (hermes-dojo). Create [src/skills/SkillMetrics.ts](src/skills/SkillMetrics.ts) with `recordInvocation(skill, outcome: "success"|"failure"|"retry"|"user-corrected", durationMs: number)` and `getMetrics(skill?): SkillStats`. Persist to `~/.gemma-code/metrics.json` with one rolling 30-day window per skill. Emit one Tracer event per invocation tagged `skill.{name}.{outcome}`. Wire into [src/skills/SkillLoader.ts](src/skills/SkillLoader.ts) skill-invocation path. Add an `/skill-metrics [name]` slash command that prints the table. Add unit tests + an integration test.

#### 5.2 -- Dual-loop curator with dry-run + rollback (items D6, D7)

**Objective**: Background timer that scans memory and skills, proposes consolidations / archives / patches, applies only after dry-run review.

**Prompt**:
> Read the comparison report Section 5a items D6 and D7 (SkillClaw + hermes-curator-evolver). Create [src/skills/CurationLoop.ts](src/skills/CurationLoop.ts) with: (1) `dryRun(): Manifest` that scans `MemoryStore` + `SkillLoader` and proposes actions (`archive-stale-skill`, `consolidate-duplicate-memory-entries`, `patch-skill-frontmatter`), writes a manifest file under `~/.gemma-code/curator/<timestamp>-dryrun.json` with full before/after preview; (2) `apply(manifestId): RollbackManifest` that executes the actions and writes a rollback manifest; (3) `rollback(rollbackId): void`. Schedule via VSCode's `setInterval` when the IDE is idle and hardware tier is `balanced` or `full` (skip on `constrained`). Default cadence: 12 hours. Add `/curate [--dry-run|--apply <id>|--rollback <id>]` slash command. Add unit tests covering dry-run idempotency + rollback round-trip.

#### 5.3 -- AST-scanned tool registry (item D2)

**Objective**: Decouple tool registration from import side effects -- scan module ASTs to detect `registry.register(...)` calls before importing.

**Prompt**:
> Read the comparison report Section 5a item D2 and the source `tools/registry.py:_module_registers_tools`. Refactor [src/tools/ToolRegistryBuilder.ts](src/tools/ToolRegistryBuilder.ts) to: (1) glob `src/tools/handlers/**/*.ts`, (2) parse each file's AST via `typescript` compiler API (already a project dep), (3) detect imports that include a `register*` symbol invocation in the module body, (4) only `import()` modules that pass the AST check. Eager-load only modules whose handlers must be active immediately; defer-load the rest until first use. Preserve the existing `ToolCatalog` projection. Add unit tests asserting that a handler with no `register()` call is NOT imported.

#### 5.4 -- 30s TTL on `check_fn` availability probes (item D3)

**Objective**: Cache the result of expensive availability probes (Docker daemon, playwright binary, Ollama reachability, MCP server presence) for 30s.

**Prompt**:
> Read the comparison report Section 5a item D3 and the source `tools/registry.py:_check_fn_cached`. Add a TTL cache to [src/tools/ToolActivationRules.ts](src/tools/ToolActivationRules.ts) keyed by check name + arguments. Default TTL = 30s. Manual invalidation API `invalidate(name?)`. Add unit tests covering: cache hit within window, cache miss after window, manual invalidate.

#### 5.5 -- `.gemma.md` git-root walk discovery (item G2)

**Objective**: Walk from cwd up to `.git` root collecting `.gemma.md` files at each level, similar to how `.gitignore` cascades.

**Prompt**:
> Read the comparison report Section 5a item G2 and the source `agent/prompt_builder.py:_find_git_root`. Extend [src/storage/MemoryFiles.ts](src/storage/MemoryFiles.ts) with `discoverGemmaContextFiles(cwd): string[]` that walks parents until either `.git` is found or the filesystem root. Each level's `.gemma.md` (if present) is added to the context, deepest-first. Apply existing `pathGuard` and `secretPaths` denylist. Wire into [src/chat/PromptBuilder.ts](src/chat/PromptBuilder.ts) after the memory snapshot but before the skill index. Add unit tests + an integration test using a temp git repo with three nested `.gemma.md` files.

#### 5.6 -- Shell-hook stdin-JSON/stdout-decision protocol (item G6)

**Objective**: Upgrade the existing 3 hooks to the more general stdin-JSON / stdout-decision contract used by hermes-agent and Claude Code, while preserving backward compatibility with the current exit-code protocol.

**Prompt**:
> Read the comparison report Section 5a item G6 and the source `agent/shell_hooks.py`. Update the three existing hooks at `scripts/hooks/{check-tool-permission, check-git-control-plane, check-prompt-policy}.mjs` to also support the new protocol: stdin receives a JSON payload with `{event, tool, args, peer, sessionId}`; stdout emits either `{"decision":"allow"}` or `{"decision":"block","reason":"..."}` (exit 0 in both cases). Legacy exit-code (0/2) protocol remains supported. Document the new protocol in `docs/harness-integration.md`. Add an idempotent registration flow: a hook running for the first time on a given session writes to `~/.gemma-code/hooks-consent.json` recording user consent. Update unit tests in `tests/unit/hooks/`. Update the v0.6.0 pen-test attack-path A simulation.

#### 5.7 -- Pre-tool command compressor (item E5)

**Objective**: Whitelist-driven rewriter that compresses long stdout from common dev commands before injection into Gemma's context.

**Prompt**:
> Read the comparison report Section 5a item E5 (rtk-hermes). Create [src/tools/handlers/preToolHook.ts](src/tools/handlers/preToolHook.ts) with a pattern table mapping command regex to a compressor function. Initial patterns: `npm test|vitest|jest|pytest` -> keep PASS/FAIL summary + first 10 errors + last 20 lines (drop test names that all passed); `git diff` -> keep file summary + first 30 lines per file; `cargo test` -> keep summary + failures; `npm install|pnpm install` -> keep summary line only. Always preserve stderr verbatim. Wire as a post-process step in [src/tools/handlers/terminal.ts](src/tools/handlers/terminal.ts) before result is returned to AgentLoop. Add a setting `gemma-code.preToolCompression = true` (default `true`). Add unit tests + a benchmark asserting >=60% token reduction on a real `vitest --run` output fixture.

#### 5.8 -- Single test runner with sub-modes (item G4)

**Objective**: One entry point for all test types with `--mode` flag.

**Prompt**:
> Read the comparison report Section 5a item G4 and the source `tests/ds4_test.c`. Create [scripts/test.mjs](scripts/test.mjs) (Node, no extra deps) that accepts `--mode=unit|integration|e2e|golden|bench|mutation|coverage|all`. Each mode maps to the existing npm scripts (`npm run test`, `npm run test:integration`, etc.). Pass-through `--watch`, `--filter`, and additional args. Update `package.json` to expose `npm run t` as a shortcut to `node scripts/test.mjs`. Update `CONTRIBUTING.md` with the new one-command test surface. Add a smoke test under `tests/unit/scripts/testRunner.test.ts` (spawn-child + smoke-check).

#### 5.9 -- lintlang-style prompt linter (item G5)

**Objective**: Static linter for skill prompts and the `src/chat/prompts/**` directory.

**Prompt**:
> Read the comparison report Section 5a item G5 (lintlang). Create [scripts/lint-prompts.mjs](scripts/lint-prompts.mjs) (Node, no extra deps) that scans `src/chat/prompts/**/*.md` and `src/skills/catalog/**/SKILL.md` and flags: (1) missing YAML frontmatter (when expected), (2) duplicate `name` across skills, (3) prompts exceeding 800 tokens (configurable), (4) hardcoded URLs (non-local), (5) PII-like patterns (emails, credit-card regexes), (6) trailing whitespace, (7) BOM markers, (8) em-dash or curly-quote presence (per the project ASCII-only rule), (9) lines wider than 500 chars (could indicate accidental binary). Output `[level] file:line message`. Exit 0 on no findings, exit 1 on warnings, exit 2 on errors. Wire into the existing `lint` npm script as `lint:prompts` and as a pre-commit hook. Add unit tests.

#### 5.10 -- [from v0.7.0 known-gaps: 10.O.5] Document MemoryPanel section-mapping decision

**Objective**: Document the static `sectionForType` mapping used by the MemoryPanel's "Promote to Memory.md" action, gather feedback during v0.8.0, finalize before release.

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.5. The MemoryPanel "Promote to Memory.md" action picks the section heading via a static mapping (`decision -> Decisions`, `preference -> Preferences`, `error_resolution -> Corrections`, `file_pattern -> Patterns`, fallback `Preferences`). Document this mapping in `docs/v0.8.0/memory-promotion-mapping.md` with rationale. Add a setting `gemma-code.memory.promotionMapping = {<type>: <section>}` that overrides the defaults. During v0.8.0 user testing, capture any feedback in `docs/v0.8.0/known-gaps.md` Section 10 and re-evaluate the mapping in Phase 7 before tagging. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 5.10".

#### 5.11 -- [from v0.7.0 known-gaps: 10.O.6] Cross-link Phase 3 context-limits work

**Objective**: Document that "Finalize per-model context limits" was completed in v0.7.0 Phase 3 sub-task 3.7 (not Phase 5 as originally planned).

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.6. Update [docs/v0.7.0/architecture.md](docs/v0.7.0/architecture.md) (or its successor) with a cross-link from the Phase 5 section to the actual Phase 3 sub-task 3.7 that shipped per-model context limits. No code change. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 5.11".

#### 5.12 -- Testing and stabilization (Phase 5)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `node scripts/test.mjs --mode=all` and confirm zero failures. Run `node scripts/lint-prompts.mjs` and confirm zero errors. Manually exercise the curator: `/curate --dry-run`, inspect the manifest, `/curate --apply <id>`, inspect rollback manifest, `/curate --rollback <rollback-id>` and confirm restoration. Verify per-skill metrics via `/skill-metrics`. Test `.gemma.md` walk with a temp nested-git-repo fixture. Update `docs/v0.8.0/known-gaps.md`. Run `/generate-session-history` to document Phase 5 at `docs/v0.8.0/development/history/phase-05.md`.

### Phase 5 Exit Checklist

- [ ] 11 sub-tasks completed
- [ ] `/skill-metrics` prints rolling 30-day stats
- [ ] Curator dry-run + apply + rollback round-trip works
- [ ] AST-scanned tool registry decouples imports
- [ ] `check_fn` TTL cache active
- [ ] `.gemma.md` walk discovers nested files
- [ ] Hooks support new stdin-JSON protocol
- [ ] Pre-tool compressor reduces tokens >=60%
- [ ] `scripts/test.mjs --mode=X` works for all modes
- [ ] Prompt linter reports zero errors on current tree
- [ ] v0.7.0 10.O.5 and 10.O.6 resolved
- [ ] Phase 5 session history written

---

## Phase 6: P2 backlog

**Goal**: Ship the remaining P2 items from the comparison report -- three-state session sync return, anticipatory context cache, reflect job, skill auto-harvest, architecture linter wrapper, per-model TTL + pinning UI, M-series tier benchmarks, tool-call exact-bytes replay, and streaming-aware tool emission formalization.

**Prerequisites**: Phase 5 complete.

**Stability Gate**: All 9 P2 items implemented with tests; existing test suite green; hybrid scoring + intuition cache + reflect job interact correctly under stress (10K events).

### Sub-tasks

#### 6.1 -- Three-state session sync return for compaction (item A4)

**Objective**: Make `ContextCompactor` return one of three explicit states so callers know when to rebuild.

**Prompt**:
> Read the comparison report Section 5a item A4 and source `ds4.h:149-167`. Edit [src/chat/ContextCompactor.ts](src/chat/ContextCompactor.ts) to return `Result = {state: "ok", summary} | {state: "error", error} | {state: "rebuild-needed", reason}`. The `rebuild-needed` state instructs the caller to discard the current conversation prefix and rebuild from the memory snapshot + recent N turns. Update all callers in `src/chat/` and `src/tools/AgentLoop.ts` to handle the new state. Add unit tests covering each return path.

#### 6.2 -- Anticipatory context cache / intuition (item A7)

**Objective**: Background prefetch of likely-relevant memory entries based on the current cursor file and recent tool history.

**Prompt**:
> Read the comparison report Section 5a item A7 (flowstate-qmd). Create [src/storage/IntuitionCache.ts](src/storage/IntuitionCache.ts) with `prefetch(signals: {currentFile?, recentTools[]}): Promise<MemoryEntry[]>` that runs the hybrid ranker (Phase 4.6) speculatively and warms an in-memory cache. Trigger on VSCode `onDidChangeActiveTextEditor` and on tool-call completion. Default warmth window = 30 seconds. Surface in MemoryPanel as a faded "anticipated context" section. Configurable via `gemma-code.memory.anticipatoryCache = false` (default `false` -- opt-in feature for v0.8.0). Add unit tests and a benchmark.

#### 6.3 -- Reflect job (item A9)

**Objective**: Nightly consolidation of session memories into durable user-profile notes in `Memory.md`.

**Prompt**:
> Read the comparison report Section 5a item A9 (hindsight retain/recall/reflect). Create [src/storage/ReflectJob.ts](src/storage/ReflectJob.ts) that runs once per day when VSCode is idle and hardware tier is `balanced` or `full`. Scan the last 24h of episodic memory; cluster by similarity (using existing `MemoryConsolidator` infrastructure); for each cluster of >=3 events, generate a one-paragraph "lesson" via a Gemma 4 call (use the curator's auxiliary call pattern from Phase 5.2 -- skipping if hardware tier is `constrained`); append to `~/.gemma-code/memory/<workspace-id>/Memory.md` under a `## Reflected Lessons` section with date stamps. Dry-run + rollback support inherited from `CurationLoop` patterns. Add unit tests + a stress test (10K events in 24h period).

#### 6.4 -- Skill auto-harvest from repeated workflows (item D4)

**Objective**: Detect n-gram tool-sequence repetition over the last 7 days; on the 3rd recurrence, propose a SKILL.md draft.

**Prompt**:
> Read the comparison report Section 5a item D4 (hermes-skill-factory). Create [src/skills/WorkflowDetector.ts](src/skills/WorkflowDetector.ts) that scans `EpisodicMemory` for tool-call sequences of length 3-7. Compute n-gram frequency; when a sequence has appeared >=3 times in the last 7 days, surface a notification in the MemoryPanel: "I noticed you ran X -> Y -> Z three times this week. Save as a skill?" On user accept, generate a SKILL.md draft (with YAML frontmatter from Phase 2.8) into `~/.gemma-code/skills/proposed/<slug>/SKILL.md`. User must explicitly move it to `~/.gemma-code/skills/<slug>/SKILL.md` to activate. Settings: `gemma-code.skills.harvest = true` (default `true`), `gemma-code.skills.harvestMinRecurrence = 3`, `gemma-code.skills.harvestWindow = 7d`. Add unit tests.

#### 6.5 -- Architecture linter wrapper (item C6)

**Objective**: Wrap dep-cruiser output in a `check-architecture.sh` + `.ps1` pair that runs in `init.sh` and CI.

**Prompt**:
> Read the comparison report Section 5a item C6 and source `projects/project-06/solution/scripts/check-architecture.sh`. Create [scripts/check-architecture.sh](scripts/check-architecture.sh) (POSIX) and [scripts/check-architecture.ps1](scripts/check-architecture.ps1) that run `npm run deps:check` (dependency-cruiser) and exit non-zero on any violation. Add a verbose mode that prints which modules violated the boundary contract. Reference both scripts from `scripts/init.sh` (Phase 2.2) so a contributor running init catches boundary issues immediately. Add a CI job. Add a smoke test.

#### 6.6 -- Per-model TTL + pinning UI in MemoryPanel (item F8)

**Objective**: Surface per-model TTL and pinning controls in the MemoryPanel webview (matches omlx's admin panel pattern but local-only).

**Prompt**:
> Read the comparison report Section 5a item F8 (omlx admin panel). Extend [src/panels/webview/memoryView.ts](src/panels/webview/memoryView.ts) with a "Models" section: list each model from the per-model context schema (Phase 4.4) plus its current TTL (time since last load), its pin state, and a manual unload button. When a model is pinned, the Ollama auto-evict on memory pressure should be respected but logged. Add three new `messages.ts` shapes: `modelPin`, `modelUnpin`, `modelUnload`. Wire into Ollama via `OLLAMA_KEEP_ALIVE` env var or the per-request `keep_alive` field. Add unit tests + an integration test against live Ollama.

#### 6.7 -- M-series tier benchmark publication (item F6)

**Objective**: Publish measured tok/s + recommended quant per Apple Silicon tier (16/24/36/64+ GB).

**Prompt**:
> Read the comparison report Section 5a item F6 and the jola.dev article (24 GB / ~40 tok/s anchor). Create `tests/benchmarks/baselines/m-series.json` with a structure `{ "machine": "M3 Pro 18GB", "model": "gemma4:e4b", "quant": "Q4_K_S", "tokensPerSecond": 38.2, "promptLatencyMs": 920, "outputLatencyMs": 26.1, "capturedAt": "2026-05-..." }` per measurement row. Capture against the developer's own machine (operator-action: requires Apple Silicon access). Add an installer-time recommendation table in `docs/v0.8.0/m-series-tier-guide.md` mapping unified-memory tier to recommended model + quant. Reference from the PyQt installer's macOS post-install flow. Update README with a "Performance on Apple Silicon" section.

#### 6.8 -- Tool-call exact-bytes replay (item E1)

**Objective**: Persist the exact rendered bytes of every tool call by ID; on replay, use the bytes verbatim instead of re-rendering.

**Prompt**:
> Read the comparison report Section 5a item E1 and source `ds4 README.md:280-310` + `misc/ANTHROPIC_LIVE_CONTINUATION.md`. Extend [src/chat/ConversationManager.ts](src/chat/ConversationManager.ts) with a `toolCallId -> rawRenderedBytes` map persisted alongside the session JSON in [src/storage/ChatHistoryStore.ts](src/storage/ChatHistoryStore.ts). On compaction replay, prefer the stored bytes over re-rendering. Bound the map at 256 entries with LRU eviction. Add unit tests covering store, retrieve, evict, replay.

#### 6.9 -- Streaming-aware tool emission formalization (item E3)

**Objective**: Send tool-call header as a single event first; stream argument bytes as deltas afterward.

**Prompt**:
> Read the comparison report Section 5a item E3 and source `ds4 README.md:269-278`. Edit [src/chat/StreamingPipeline.ts](src/chat/StreamingPipeline.ts) to formalize the header-first contract: on detecting the start of a tool-call tag, emit one `tool-call-header` event with the parsed tool name; then stream the argument bytes as `tool-call-arg-delta` events; on tag close, emit `tool-call-complete`. Update the webview render protocol to render tool-call cards progressively. Add unit tests covering: header before any deltas, multiple tool calls in one stream, malformed/incomplete tool-call recovery.

#### 6.10 -- Testing and stabilization (Phase 6)

**Objective**: Generate and run all tests for this phase. Iterate until stable.

**Prompt**:
> Run `node scripts/test.mjs --mode=all`. Confirm zero failures. Manually exercise: enable anticipatory cache, switch between files, observe prefetch latency; run the reflect job manually with `/reflect --dry-run`; trigger a workflow-detector match (run a 3-step sequence 3 times); pin a model and confirm it survives idle eviction. Capture M-series benchmark on developer's own machine and commit `m-series.json`. Update `docs/v0.8.0/known-gaps.md`. Run `/generate-session-history` to document Phase 6 at `docs/v0.8.0/development/history/phase-06.md`.

#### 6.A -- [from v0.7.0 known-gaps: 10.O.7] Cursor adapter native `.mdc` rule emission

**Objective**: Replace the placeholder `rule: SKILL` frontmatter in the Cursor skill-export adapter with native `.cursor/rules/<slug>.mdc` shape (`description` / `globs` / `alwaysApply`).

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.7. Locate the Cursor adapter in `src/skills/adapters/cursor.ts` (or its v0.7.0 successor). Update the emitter so that exported skills produce `.cursor/rules/<slug>.mdc` with frontmatter: `description: <SKILL.description>`, `globs: ['**/*']` (or skill-declared globs if present), `alwaysApply: false`. Body is the SKILL.md body verbatim. Migrate the existing bundled `.cursor/rules/<slug>.md` outputs and update the README of the bundled export. Add unit tests covering the new shape. Move the item from Open to Resolved in `docs/v0.8.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 6.A".

### Phase 6 Exit Checklist

- [ ] 9 sub-tasks completed
- [ ] Three-state sync return integrated into AgentLoop
- [ ] Anticipatory cache opt-in and functional
- [ ] Reflect job runs nightly when conditions are met
- [ ] Workflow detector proposes skills on 3rd recurrence
- [ ] `check-architecture.sh`/`.ps1` integrated into init + CI
- [ ] Per-model pin/unpin UI works
- [ ] M-series benchmark JSON committed
- [ ] Tool-call exact-bytes replay correct
- [ ] Streaming-aware emission formalized
- [ ] Phase 6 session history written

---

## Phase 7: Polish, golden re-capture, security review, release

**Goal**: Final cycle close. Re-run all baselines against the post-Phase-6 build; full pen-test re-run; ADR cross-reference fix; CHANGELOG update; ship v0.8.0.

**Prerequisites**: Phase 6 complete.

**Stability Gate**: Full pen-test re-run produces zero new findings; mutation score >=v0.7.0 (50.64%); coverage >=80%; v0.8.0 baselines captured; CHANGELOG updated; v0.8.0 tag exists on origin/main; post-tag exit verification passes.

### Sub-tasks

#### 7.1 -- [from v0.7.0 known-gaps: 10.O.4] Update plan ADR cross-references

**Objective**: Fix the ADR numbering deviation noted in v0.7.0 known-gap 10.O.4 (Phase 5 plan referenced ADR-0007 but the new ADR landed as ADR-0014).

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.4. Update the v0.7.0 cycle plan at [docs/v0.7.0/plans/v0.7.0-cycle.md](docs/v0.7.0/plans/v0.7.0-cycle.md) cross-references to ADRs to match the actually-shipped numbering: Phase 4 ADR is 0013 (not 0008); Phase 5 ADR is 0014 (not 0007). Also update Phase 2's new ADRs introduced in v0.8.0 (0015 pass-state-gating, 0016 second-llm-backend) with their final numbers in `docs/v0.8.0/plans/v0.8.0-cycle.md` if any drift occurred during implementation. Move the item from Open to Resolved in `docs/v0.7.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 7.1".

#### 7.2 -- Final mutation testing pass

**Objective**: Run Stryker over the v0.8.0 critical modules and ensure mutation score has not regressed.

**Prompt**:
> Run `npm run mutate` against the focused runner config in `configs/stryker.config.json`. Compare against v0.7.0 baseline (50.64% overall, 58.92% covered). Target: >=v0.7.0 score. If regression, identify the top 5 newly-surviving mutants and either add targeted regression tests or document the rationale in `docs/v0.8.0/known-gaps.md` Section 4. Update the report at `docs/v0.8.0/review/mutation-report.md`.

#### 7.3 -- Re-capture v0.8.0 golden + benchmark baselines

**Objective**: Capture the post-Phase-6 build's baselines for use in v0.9.0.

**Prompt**:
> Run `npm run bench -- --update-baseline` to write `tests/benchmarks/baselines/v0.8.0.json`. Run `python tests/golden/framework/run_all.py --model gemma4:e4b --output tests/golden/baselines/v0.8.0.json` against live Ollama. Run `scripts/check-bench-regressions.mjs` against v0.7.0 and v0.6.0 baselines. Document the deltas in `docs/v0.8.0/performance-baselines.md`. Confirm the >=40% token-savings claim from v0.5.0 retrospective still holds; if not, add a new retrospective note.

#### 7.4 -- Full pen-test re-run

**Objective**: Re-run the v0.6.0 pen-test attack paths against the v0.8.0 source.

**Prompt**:
> Read `docs/v0.6.0/review/penetration-test.md`. Re-run Attack Paths A, B, C against the current v0.8.0 source. Specifically validate: (a) the prompt-injection scanner (Phase 2.7) blocks the new attack vectors from learn-harness-engineering L09 (agent self-attestation), (b) the pass-state gate (Phase 2.4) blocks "I'm done" without verification, (c) the trace file (Phase 4.1) redacts secret paths correctly, (d) the curator (Phase 5.2) cannot delete user-pinned skills or memory entries. Document findings in `docs/v0.8.0/review/penetration-test.md`. Any P0/P1 findings block release.

#### 7.5 -- CHANGELOG and README updates

**Objective**: Update CHANGELOG.md with the full v0.8.0 change list and README.md with the new commands + settings.

**Prompt**:
> Run `/generate-changelog` to draft the v0.8.0 entry from git log. Edit [CHANGELOG.md](CHANGELOG.md) to include: all skill-native additions (Phase 1), harness artifacts (Phase 2), plan-mode overhaul (Phase 3), LM Studio backend (Phase 4), curator + hooks + linter (Phase 5), P2 backlog items (Phase 6). Update README.md sections: "Features" (add LM Studio + thinking modes + plan annotation + curator), "Slash Commands" (add `/lens`, `/incident-commander`, `/council`, `/trace`, `/thinking-mode`, `/curate`, `/skill-metrics`, `/reflect`), "Configuration" (add new settings: `llm.backend`, `lmstudio.baseUrl`, `passStateGating`, `memorySnapshotMode`, `memory.scoringMethod`, `memory.anticipatoryCache`, `preToolCompression`, `skills.harvest*`, `lmstudio.*`). Confirm ASCII-only content.

#### 7.6 -- Ship v0.8.0

**Objective**: Tag, build VSIX, publish GitHub release.

**Prompt**:
> Update `package.json` version to `0.8.0`. Run `node scripts/test.mjs --mode=all` and confirm zero failures. Run `npm run build && npx vsce package --no-dependencies` to produce `gemma-code-0.8.0.vsix`. Commit `chore(release): v0.8.0` with the VSIX referenced in the commit body. Tag `git tag v0.8.0`. Push tag to origin. Create GitHub release with the VSIX attached and the CHANGELOG v0.8.0 entry as the release body. Run the post-tag exit verification (Phase 0 sub-task 0.6 procedure but against v0.8.0). Finalize `docs/v0.8.0/known-gaps.md` Status from `in-progress` to `finalized`.

#### 7.A -- [from v0.7.0 known-gaps: 10.O.8] Add `no-bare-promise-rejection` lint rule

**Objective**: Ship the optional 5th `gemma-check` rule that flags `.catch()` with no argument.

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.8. Add `lib/checks/no-bare-promise-rejection.mjs` mirroring the existing rule contract from the 4 shipped rules. Pattern: `\.catch\(\s*\)` -- a bare `.catch()` invocation with no handler. Add a fix suggestion: "Add a handler that at minimum logs the error". Wire the rule into the `gemma-check` runner and the `lint:prompts` script (Phase 5.9). Add 3 unit tests. Move the item from Open to Resolved in `docs/v0.8.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 7.A".

#### 7.B -- [from v0.7.0 known-gaps: 10.O.9] Resolve 4 pre-existing dep-cruiser violations

**Objective**: Either relax the `no-storage-from-panels` and `no-panels-from-tools` rules with documented inline exceptions, or refactor the violating modules to comply.

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.9. Run `npm run deps:check` and confirm the 4 violations. For the 3 `no-storage-from-panels` violations (MemoryPanel importing MemoryStore / MemoryShared.types / MemoryFiles): MemoryPanel owns the store by design, so add inline `dependency-cruiser-disable-next-line no-storage-from-panels` markers with an explanation comment, AND update `configs/dependency-cruiser.cjs` rule documentation to note MemoryPanel as the canonical exception. For the 1 `no-panels-from-tools` violation (ConfirmationGate importing webview/render/permissionPrompt): refactor ConfirmationGate to receive a prompt-renderer callback function via its constructor; move the existing import to the call site in panel bootstrap. Add a regression test asserting `npm run deps:check` exits 0. Move the item from Open to Resolved in `docs/v0.8.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 7.B".

#### 7.C -- [from v0.7.0 known-gaps: 10.O.10] Clean up `console.log` in legacy scripts

**Objective**: Convert `console.log` calls in legacy scripts to `process.stdout.write` to match project style and pass `gemma-check`.

**Prompt**:
> Read `docs/v0.7.0/known-gaps.md` Section 10 row 10.O.10. Convert `console.log` calls in `scripts/check-bench-regressions.mjs`, `scripts/hooks/check-prompt-policy.mjs`, and `scripts/hooks/lib/secret-paths.mjs` to `process.stdout.write` (with explicit newlines). Preserve all formatting and error semantics. For pattern-definition lines in `secret-paths.mjs` that legitimately need the literal string, add a `gemma-check-allow` marker with a one-line rationale. Run `node scripts/test.mjs --mode=unit` and `gemma-check` to confirm zero new findings. Move the item from Open to Resolved in `docs/v0.8.0/known-gaps.md` noting "Resolved in v0.8.0 Phase 7.C".

#### 7.7 -- Testing and stabilization (Phase 7)

**Objective**: Final stabilization before tag. Iterate until stable.

**Prompt**:
> Run the full ship gate: `npm ci && npm run lint && npm run build && npm run test && npm run test:integration && npm run bench && npm run deps:check && npm run catalog:check && npm run perm-tier:check && npm audit --production --audit-level=moderate && node scripts/lint-prompts.mjs && bash scripts/check-architecture.sh`. All must exit 0. If any fails, fix root-cause, do not bypass. Run `/generate-session-history` to document Phase 7 at `docs/v0.8.0/development/history/phase-07.md`. Run `/wrap-up-session` to produce `session-handoff.md` and `session-progress.md` for the cycle close.

### Phase 7 Exit Checklist

- [ ] All sub-tasks completed
- [ ] Mutation score >= v0.7.0
- [ ] Coverage >= 80%
- [ ] v0.8.0 golden + benchmark baselines captured
- [ ] Pen-test zero P0/P1 findings
- [ ] ADR cross-references corrected
- [ ] CHANGELOG.md + README.md updated
- [ ] v0.8.0 tag exists on origin/main
- [ ] VSIX published in GitHub release
- [ ] Post-tag exit verification passes
- [ ] `docs/v0.8.0/known-gaps.md` finalized
- [ ] Phase 7 session history written

---

## Items explicitly NOT adopted (security / policy reasons)

The MCP Registry Policy in [AGENTS.md](../../AGENTS.md) gates these. All are classified `drop-outright`. They are recorded here, not in the adoption plan above.

- **N1 Messaging gateways** (Telegram, Discord, Slack, WhatsApp, Signal -- S1 hermes-agent `gateway/run.py`). Reason: outbound calls to third-party messaging services violate the local-only thesis (Hard Constraint #1). MCP Registry Policy bucket 5.
- **N2 Cloud terminal backends** (Modal, Daytona, Vercel Sandbox, Singularity -- S1 `tools/environments/`). Reason: sends code to remote sandboxes; violates local-only thesis. Bucket 5.
- **N3 Atropos RL training harness** (S1 `environments/`). Reason: multi-GB ML training stack with cloud connector; out of scope for an editor extension. Bucket 5.
- **N4 DSPy / GEPA prompt evolution** (S1 hermes-agent-self-evolution). Reason: heavy ML dependency stack; offline prompt tuning value does not justify the dep weight on a local Ollama path. Bucket 5.
- **N5 External memory provider services** (Honcho, Hindsight-as-a-service, Mem0 -- S1 `agent/memory_provider.py` bundled adapters). Reason: these ship as cloud services or self-hosted servers; the abstract MemoryProvider ABC is fine but bundling cloud adapters is not. Bucket 5. *Note: the patterns from Mnemosyne, yantrikdb, hindsight ARE adopted but reverse-engineered locally in Phase 4.6, Phase 6.3.*
- **N6 MCP OAuth manager** (S1 `tools/mcp_oauth_manager.py`). Reason: OAuth flows assume browser callbacks to cloud auth servers. Local MCP stdio support stays; OAuth path drops. Bucket 5.
- **N7 Plannotator E2E-encrypted plan-share service** (S7 `~/.plannotator/sessions/` with paste service). Reason: outbound network share, even encrypted, violates local-only thesis. Bucket 5. *Note: local plan archive (Phase 3.2) is adopted.*
- **N8 Cost-estimation / usage_pricing** (S1 `agent/usage_pricing.py`). Reason: meaningless for local Ollama; would only add maintenance burden. Bucket 5 (no value).
- **N9 Curator with always-on auxiliary inference** (S1 `agent/curator.py` running an aux model continuously). Reason: contends with main inference on a single-GPU laptop. Bucket 5 (cost/benefit fails). *Note: the lifecycle pattern IS adopted but gated by hardware tier and run on a 12h timer with dry-run default in Phase 5.2.*
- **N10 Blockchain / crypto-payment skills** (hermes-payguard, USDC/x402 wallet, hermes-blockchain-oracle, ripley-xmr-gateway -- S5). Reason: off-thesis for a local coding agent. Bucket 5.
- **N11 Voice / media / robotics skills** (resemble-ai/detect-skill, hermes-embodied, mars-rover, hermescraft -- S5). Reason: out of editor-extension scope. Bucket 5.

Additionally:

- **OmlxClient (S6 omlx as third LLMClient)**: NOT adopted in v0.8.0; deferred to v0.9.0+. Reason: alpha-stage maturity (`Development Status :: 3 - Alpha` in pyproject; 30 point releases in 3 months pinning specific git commits); single-maintainer (898/970 commits = bus factor 1). LM Studio (Phase 4.2) provides the Apple-Silicon path in the meantime. Re-evaluate when omlx reaches v0.5+.
- **Split-sampling (S4 item E2)**: NOT adopted in v0.8.0. Reason: Ollama exposes per-request sampling, not per-token sampling control; implementation would require either provider-level support or migration away from Ollama. Out of v0.8.0 scope. Re-evaluate if/when Ollama exposes per-token sampling controls or if we add a direct llama.cpp/llama-server backend.
- **Logprob-vector regression test (S4)**: NOT adopted in v0.8.0. Reason: Ollama logprob fidelity uncertain across model versions; needs an investigation phase before commitment.

---

## Source Comparison Reference

Generated from [docs/v0.7.0/comparison-multi-source-v2.md](../../v0.7.0/comparison-multi-source-v2.md) on 2026-05-14.

- 7 external sources analyzed (1 article, 6 repos)
- 38 adoption candidates surfaced
- 18 P0/P1 items consolidated into Phases 1-4
- 9 P2 items in Phase 6
- 11 items explicitly NOT adopted (Items N1-N11 above)
- 10 v0.7.0 known-gaps items ingested and tagged `[from v0.7.0 known-gaps: ...]` (3 in Phase 0, 2 in Phase 5, 1 in Phase 6, 4 in Phase 7)

Total estimated cycle: ~85 days from Phase 0 to v0.8.0 tag.
