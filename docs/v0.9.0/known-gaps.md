# v0.9.0 -- Known Gaps, Deferrals, and Carryovers

**Status**: in-progress (the cycle is closed for v0.9.0 -- Phase 8 shipped on 2026-05-16; this file will be flipped to `finalized` at v0.10.0 cycle close, mirroring the v0.8.0 -> v0.9.0 transition handled by Phase 8.3)
**Audience**: v0.9.0 phase authors, code reviewer, security reviewer, ops engineer running the live-Ollama capture
**Sibling reviews**: [docs/v0.8.0/known-gaps.md](../v0.8.0/known-gaps.md) (the in-cycle gap log this file inherits from); [docs/v0.9.0/plans/v0.9.0-cycle.md](plans/v0.9.0-cycle.md) (the active plan); [docs/v0.9.0/operator-actions.md](operator-actions.md) (the eight operator-only items lifted from v0.8.0 Section 10.1).
**Context**: This file mirrors `docs/v0.8.0/known-gaps.md`'s structure. It is appended phase-by-phase as v0.9.0 lands. Each entry records the source phase, plan reference, category, severity, reason, and suggested next step. Items move to `## Resolved` when closed in a later phase, and the `## Summary` at the bottom is recomputed each pass.

Each entry has a severity tag:

- **P0** -- release-blocker for v0.9.0 (must close)
- **P1** -- should-fix in v0.9.0
- **P2** -- nice-to-have; documented for completeness
- **P3** -- out-of-scope for v0.9.0; explicitly recorded for future planning

Each entry has a category tag:

- **NI** (not implemented) -- a plan sub-task that was skipped
- **DF** (deferred) -- a plan sub-task explicitly deferred to a later phase / cycle
- **BG** (bug) -- a deviation that revealed a real defect
- **MT** (missing tests) -- a coverage shortfall
- **WN** (warning) -- a suppressed lint or runtime warning
- **QG** (quality gate) -- a Phase 7 gate the cycle author bypassed with "Proceed anyway"

---

## 1. Operator-action items

Eight items in v0.8.0 Section 10.1 required authorized operator execution (live-Ollama capture, mutation re-run, pen-test re-run, release tag publication). All eight (10.O.A / B / C / X / AA / BB / CC / DD) are tracked verbatim in [docs/v0.9.0/operator-actions.md](operator-actions.md). When the operator flips each section's status to `done`, a one-line Resolved row also appears in Section 10.2 below.

---

## 10. v0.9.0 in-cycle gap log

**Last updated**: 2026-05-16 (Phase 8 close: 8.1 README + AGENTS.md updates, 8.2 catalog regen, 8.3 v0.8.0 known-gaps finalized, 8.4 this file, 8.5 DEVLOG cycle entry, 8.6 deferred per 10.O.EE, 8.7 committed-and-pushed-to-main per user override).

### 10.1 Open Items

| ID | Source phase | Plan reference | Category | Severity | Reason | Suggested next step |
|---|---|---|---|---|---|---|
| 10.N.A | v0.9.0 Phase 2.7 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.7 | DF | P3 | `ModelPinRegistry` is instantiable and `StreamingPipeline` accepts a `KeepAliveResolver` callback, but no production composition root currently constructs the registry or wires the resolver. The plumbing is non-breaking (omitting the resolver preserves Ollama's 5-minute default), so pinned-model behaviour will not take effect until a Phase 6 wiring landingstep grafts the registry into `ChatPanelBootstrap`. | Phase 6 (or earlier): construct `ModelPinRegistry` in `ChatPanelBootstrap`, persist pins via `vscode.Memento.workspaceState`, and pass `(model) => registry.keepAliveFor(model)` into `ChatController.buildStreamingPipeline`. |
| 10.N.B | v0.9.0 Phase 2.9 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.9 | DF | P3 | The backend now emits `toolCallHeader` / `toolCallArgDelta` / `toolCallComplete` reliably, but the webview's tool-call card (`src/panels/webview/render/toolCallCard.ts`) still renders the legacy `toolUse` / `toolResult` shape. The protocol union already carries the three new types so a webview-only update can land them progressively. Phase 4 was scoped to dev-loop ergonomics (debug runners, diff-cover, pre-push, work dispatcher) and did not touch the webview. | Phase 6 "UX polish": update `toolCallCard.ts` to switch on the three new event types and render a header-spinner -> arg-stream -> result-preview layout. No backend changes required. |
| 10.N.C | v0.9.0 Phase 2.4 / 2.6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-tasks 2.4, 2.6 | DF | P3 | `MemoryPanel` exposes the new `anticipated` and `proposedSkills` fields on its snapshot but the webview's memory-view HTML (`src/panels/webview/memoryView.ts`) has not been extended yet to render the two new sections or wire the Inspect / Accept / Dismiss buttons. The host-side handlers are in place; only the webview templates and JS need to consume the new fields. Phase 4 was scoped to dev-loop ergonomics and did not advance webview rendering. | Phase 6 "UX polish": extend `getMemoryViewHtml` to render an "Anticipated context" faded section and a "Proposed skills" action row, posting `inspectProposedSkill` / `acceptProposedSkill` / `dismissProposedSkill` to the host. |
| 10.N.D | v0.9.0 Phase 2.10 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.10 | DF | P2 | Phase 2's "Latency regression check" (`npm run bench` rendering + retrieval p50 vs. v0.8.0 baselines) and "Manual smoke (Windows, real Ollama)" both require live inference on a quiescent workstation; the agent is not authorized to drive live Ollama. The benches do not run as part of `npm test` and were not executed in the Phase 2 implementation pass. | Operator: run `npm run bench` after the v0.8.0 baseline file is captured (see operator-actions.md Section 1) and compare rendering + retrieval p50 deltas to v0.8.0; if any sub-task introduces >10% regression, file a follow-up tracked here. |
| 10.N.E | v0.9.0 Phase 2.10 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.10 | NI | P3 | Phase 2 deviated from the "Atomic commits per sub-task" guidance: the nine wirings landed as one consolidated commit because the changes share types (`SubAgentType`, `MemorySnapshotMessage`) and tests cross-reference each other. The plan called for ~9 atomic commits; the user's invocation explicitly requested a single commit + push to main, which overrides that guidance. Phase 3 made the same trade-off for the same reason (single user-requested commit + push to main covering all five Phase 3 artifacts). | Future phases that touch independent files should keep the atomic-commit pattern when the user does not explicitly request a single commit. No follow-up needed for Phase 2 or Phase 3. |
| 10.N.F | v0.9.0 Phase 3.1 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 3.1 | DF | P3 | The plan's stated cap for the rewritten `review-pr` SKILL was "under 8 KB to avoid `prompt-oversized`", but the rule's actual budget is 800 approximate-tokens (`lib/checks/prompt-oversized.mjs`, `chars/4` heuristic), which is much tighter than 8 KB. To clear the rule warning-free the SKILL was trimmed to ~800 tokens, which is more terse than the plan's "rich CodeRabbit-style structure" ideal. The reverse-engineered pattern (walkthrough / per-file analysis / severity-x-confidence / review-first then apply) is intact but the prose is compressed. | Phase 6.8 already tracks the broader oversized-SKILL trim (10.O.O carryover). When that work lands, re-evaluate whether the `prompt-oversized` budget should be raised in `GEMMA_CHECK_PROMPT_TOKEN_BUDGET` so SKILL prompts can carry more guidance; if so, expand the review-pr SKILL again. |
| 10.N.H | v0.9.0 Phase 3.6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 3.6 | DF | P3 | The plan's Phase 3.6 step "extend gemma-check to scan `.claude/` if not yet supported; file the extension as a v0.10.0 candidate if non-trivial" was not exercised: `gemma-check` already accepts directory arguments (`node bin/gemma-check.mjs .claude` runs cleanly with 0 findings on the new files), so the `prompt-*` rules' `appliesTo` gate is the only thing limiting coverage. The rules currently match `src/chat/prompts/` and `src/skills/catalog/.../SKILL.md`; the new `.claude/agents/*.md` and `.claude/commands/*.md` files are scanned for ASCII / whitespace / BOM but not for `prompt-oversized`. | v0.10.0 candidate: extend `lib/checks/prompt-oversized.mjs.isPromptOrSkillMarkdown` (and the other prompt-* rules) so `.claude/agents/*.md` and `.claude/commands/*.md` are also gated against the 800-token budget. Mirrors the existing Phase 6.8 / 10.O.O trim work. |
| 10.N.I | v0.9.0 Phase 4.2 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 4.2 | DF | P3 | The `coverage-diff.yml` workflow is wired and the markdown / HTML reports plus the PR-comment-on-failure path are in place, but the plan's acceptance criterion "synthetic PR with intentional coverage drop fails the workflow + posts the markdown comment; noop PR passes" requires opening two real PRs against `main` and watching the gate fire. The CI workflow file is committed and lint-clean; the smoke validation moves to the operator after Phase 4 lands. | Operator: open one synthetic PR that drops changed-lines coverage to ~70% (e.g. adds an untested helper) and confirm the gate fails + the markdown comment is posted; open one noop PR and confirm the gate passes; close both without merging. |
| 10.N.J | v0.9.0 Phase 4.3 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 4.3 | MT | P3 | `tests/integration/husky-prepush.test.ts` asserts the hook's textual contract (POSIX shebang, `set -e`, the five steps in the right order, no `\|\| true` muting on lint/build/check) but does not spawn the hook against a real branch. Running the hook end-to-end inside `npm test` would re-enter the same vitest invocation and would add a 30+ second build round-trip per `npm test`. The plan's "manual smoke on real branch" acceptance criterion is therefore operator-driven. | Operator: on a feature branch with a deliberate lint violation (e.g. add an unused variable), run `bash .husky/pre-push origin main` and confirm exit 1 + the re-stage / re-push prompt; clear the violation and re-run, confirm exit 0. |
| 10.N.K | v0.9.0 Phase 4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 4.5 | NI | P3 | Phase 4 deviated from the plan's "Atomic commits per artifact" guidance (one commit per artifact) for the same reason Phase 2 and Phase 3 did: the user explicitly requested a single commit + push to main covering all four Phase 4 artifacts. The four artifacts touch different files (`scripts/debug/`, `.github/workflows/coverage-diff.yml`, `.husky/pre-push`, `scripts/work.mjs`) and the bundled commit is mechanically clean but does not match the plan's atomic-commit ideal. | Future phases that touch independent files should keep the atomic-commit pattern when the user does not explicitly request a single commit. No follow-up needed for Phase 4. |
| 10.N.L | v0.9.0 Phase 5.1 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 5.1 | DF | P3 | The deep-work CLI ships the full lifecycle (`start` / `pick` / `continue` / `status` / `list` / `cleanup`) and is exercised by 19 integration-tier tests in `tests/integration/scripts-deep-work.test.ts`, but the plan's "lifecycle works on a synthetic issue" acceptance criterion requires a real authenticated `gh` session to run `gh issue view` / `gh issue list` against the live repo. The unit-level surface (slugify / deriveBranchName / parseWorktreeListPorcelain / buildDeepWorkPrompt / formatIssueMenu / formatWorktreeTable / cli --help) is fully covered; the gh / git lifecycle smoke is operator-driven. | Operator: pick a low-impact open issue, run `npm run deep-work start <num>`, confirm worktree appears at `worktrees/issue-<num>-<slug>` with the prompt printed; `npm run deep-work status` lists it; `npm run deep-work cleanup <num>` removes it cleanly. Record the run in `docs/v0.9.0/operator-actions.md`. |
| 10.N.M | v0.9.0 Phase 5.2 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 5.2 | DF | P3 | `scripts/check-pr-checklist.mjs`, the PR template, and `.github/workflows/pr-quality.yml` are all wired and the 7-test unit suite at `tests/unit/scripts/check-pr-checklist.test.ts` exercises the parser end-to-end against synthetic PR bodies, but the plan's "synthetic PR with unchecked + non-N/A item -> workflow fails; with `N/A:` -> passes" acceptance criterion still requires opening real PRs to watch the gate fire on GitHub-Actions infrastructure. The workflow file is committed lint-clean and the script behaves correctly under direct `PR_BODY=... node scripts/check-pr-checklist.mjs` invocation. | Operator: open one synthetic PR with the template's Submission Checklist left intentionally unchecked (no `N/A:` reasons); confirm the `pr-quality.yml` Submission Checklist gate fails. Update the PR body to mark every box `- [x]` or `- [ ] N/A: <reason>`; confirm the gate passes. Close without merging. |
| 10.N.N | v0.9.0 Phase 5.3 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 5.3 | DF | P3 | The agent-batch CLI ships `validate` / `overlap` / `launch` / `status` plus a Zod schema and the canonical `examples/agent-batch.spec.json` sample, exercised by 21 unit tests. `launch` defaults to dry-run (no side effects) and only creates worktrees when `--apply` is passed, at which point it delegates to `scripts/deep-work/start.mjs` (which needs `gh` + `git`). The plan's "spec validates; overlap detection works" acceptance is met by the unit suite; the live `--apply` dispatch is operator-driven because spawning three agent worktrees over real issues is not safe to do inside `npm test`. | Operator: pick three real low-impact issues, edit `examples/agent-batch.spec.json` to reference them, run `npm run agent-batch validate examples/agent-batch.spec.json`, then `overlap`, then `launch examples/agent-batch.spec.json --apply` to dispatch worktrees. Verify each task appears in `npm run deep-work status`. |
| 10.N.O | v0.9.0 Phase 5.4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 5.4 | DF | P3 | The review CLI ships `sync` / `review` / `fix` / `coverage` / `merge` and is exercised by 20 integration-tier tests covering the pure logic (parseGhPrChecks, summarizeChecks, extractUncoveredFromMarkdown, suggestTestFiles, formatCoverageReport, summarizeReviewerComments, --help / unknown / --dry-run paths). The plan's "each sub-command works in dry-run" acceptance is met. The live `sync` / `merge` paths still require a real authenticated `gh` session against an open PR; both are operator-driven. The explicit overlap with `/ship-and-babysit` is documented in `scripts/review/cli.mjs` and `scripts/review/shared.mjs` headers, with the "fold one if usage converges" note for v0.10.0. | Operator: on a real PR, exercise `npm run review sync <pr>` (with a clean tree), `npm run review coverage <pr>` (after `coverage-diff.yml` runs), and `npm run review merge <pr> --dry-run`. Decide at v0.10.0 whether the imperative `review` CLI or the autonomous `/ship-and-babysit` slash command should fold based on the cycle's PR-flow telemetry. |
| 10.N.P | v0.9.0 Phase 5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 5.5 | NI | P3 | Phase 5 deviated from the plan's "Atomic commits per artifact" guidance (one commit per artifact) for the same reason Phases 2, 3, and 4 did: the user explicitly requested a single commit + push to main covering all four Phase 5 artifacts. The four artifacts touch different directories (`scripts/deep-work/`, `scripts/check-pr-checklist.mjs` + `.github/`, `scripts/agent-batch/` + `examples/`, `scripts/review/`) and the bundled commit is mechanically clean but does not match the plan's atomic-commit ideal. | Future phases that touch independent files should keep the atomic-commit pattern when the user does not explicitly request a single commit. No follow-up needed for Phase 5. |
| 10.N.Q | v0.9.0 Phase 6.1 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.1 | DF | P3 | `IdleTimeScheduler` is a pure module with full unit-test coverage but is **not yet wired** into `ChatPanelBootstrap` -- no production composition root constructs the scheduler or registers the curator / reflect workers against it. Plumbing required: instantiate the scheduler at bootstrap with the VSCode-backed `activitySource` (subscribing to `vscode.workspace.onDidChangeTextDocument` + `vscode.window.onDidChangeActiveTextEditor`), register the two workers with their idle / cadence thresholds (curator 5 min / 12 h, reflect 10 min / 24 h), and call `start()` after panel activation. The legacy edit-trigger gating in `AgentLoop._runOneIteration` continues to dispatch curator / reflect from the post-N-edits trigger as a safety fallback until the scheduler is wired. | Phase 8 cycle close (or a v0.10.0 follow-on): instantiate `IdleTimeScheduler` in `ChatPanelBootstrap`, register curator + reflect, and delete the legacy curator-cadence fallback in `AgentLoop` once the scheduler-driven path has telemetry behind it. |
| 10.N.R | v0.9.0 Phase 6.6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.6 | DF | P3 | The lazy-import driver in `ToolRegistry.registerLazy` + `ToolRegistryBuilder.buildToolRegistry` defers handler-class instantiation for tier `confirm` / `dangerous` tools, but the underlying handler modules (`./handlers/terminal.js`, `./handlers/webSearch.js`) are still imported transitively by `PermissionTiers.ts.isAllowlisted`, `ActionClassifier.ts.isBlocked`, `McpClient.ts.stripHtmlTags`, and `SubAgentManager.ts`. So the boot-time `require()` count is unchanged in practice -- the lazy mechanism is correct, but its full benefit waits on extracting the shared utilities (`isAllowlisted`, `isBlocked`, `stripHtmlTags`) into smaller pure modules so the handler files themselves can stay un-imported at boot. The unit test for the lazy mechanism (`tests/unit/tools/ToolRegistry.test.ts`) and the integration test for the builder (`tests/integration/tools/lazy-import-builder.test.ts`) both pass. | v0.10.0: split `src/tools/handlers/terminal.ts` into `terminal-utils.ts` (export `isAllowlisted`, `isBlocked`) + `terminal-handler.ts` (export `RunTerminalTool`), and split `src/tools/handlers/webSearch.ts` similarly. After the split, the handler files import only their own concerns and the lazy gate genuinely shaves boot-time imports. |
| 10.N.S | v0.9.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.9 | NI | P3 | Phase 6 deviated from the plan's "Atomic commits per sub-task" guidance for the same reason Phases 2 through 5 did: the user explicitly requested a single commit + push to main covering all 8 Phase 6 artifacts. The artifacts touch different directories (`src/agents/IdleTimeScheduler.ts`, `src/tools/AgentLoop.ts`, `src/panels/ChatCommandHandlers.ts`, `src/storage/PlanArchive.ts`, `src/chat/ImprovementHook.ts`, `src/tools/ToolRegistry.ts` + `ToolRegistryBuilder.ts`, `tests/integration/llm/LmStudioClient.live.test.ts`, `src/skills/catalog/{harden,distill,build-second-brain,animate}/SKILL.md`) and the bundled commit is mechanically clean but does not match the plan's atomic-commit ideal. | Future phases that touch independent files should keep the atomic-commit pattern when the user does not explicitly request a single commit. No follow-up needed for Phase 6. |
| 10.N.T | v0.9.0 Phase 7.2 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.2 | MT | P3 | Global function coverage (88.94%, 1199 / 1348) clears the new `functions >= 80` gate in `.github/workflows/ci.yml` `coverage-gate`, but 31 individual files still sit below the per-file 80% function-pct floor (top offenders: `src/panels/TraceDashboardPanel.ts` 25%, `src/panels/MemoryPanel.ts` 37.5%, `src/panels/ChatPanelBootstrap.ts` 39.5%, `src/panels/ChatMessageRouter.ts` 40%, `src/panels/GemmaCodePanel.ts` 48.5%; plus 12 `*.types.ts` files at 0% which are essentially type-only shims). The aggregate gate is what protects against regression; per-file backfill is cost-benefit gated because the panels are VSCode-host wired (heavy mocking) and the `*.types.ts` files are intentionally one-export-symbol modules. | v0.10.0 candidate: write panel integration tests against the VSCode test runner for the five panel files above, or alternatively add an `exclude` glob for `*.types.ts` to the coverage config (those files are type aliases with no runtime behaviour). The `*.types.ts` exclusion would lift the global to ~90% and remove the 12 false-positive entries from the per-file low-coverage list. |
| 10.N.U | v0.9.0 Phase 7.4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.4 | DF | P3 | `.github/workflows/codeql.yml` runs with `continue-on-error: true` on the analyze job per the plan's "non-blocking first" stance. The acceptance criterion "after 1 week with no critical/high findings, flip to blocking on critical+high" is operator-driven: a week of green CodeQL runs on `main` is needed before the flag flips. The SARIF report uploads to the GitHub Security tab on every run so the baseline is visible without a gating change. | Operator: review the CodeQL Security tab after one week of clean main-branch runs (`security-and-quality` query pack on `javascript-typescript`); if zero critical+high findings, flip `continue-on-error: true` -> `false` on the `analyze` job and tighten the query pack to `security-extended`. |
| 10.N.W | v0.9.0 Phase 7.7 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.7 | DF | P3 | The plan's Phase 7.7 "Open a PR that touches a file in `src/`; verify all new jobs fire" acceptance is operator-driven: it requires a real PR against `main` to watch the v0.9.0 Phase 7 jobs (Node 22 + 24 matrix, functions coverage gate, check-prompts, CodeQL, fast-bench, dep-graph SVG artifact) fire end-to-end on GitHub-Actions infrastructure. The workflow files are committed and lint-clean; full local validation is constrained to YAML-level checks plus the `tests/unit/workflow-discipline.test.ts` SHA-pinning gate. | Operator: open one synthetic PR that modifies a file under `src/` (e.g. a trivial doc-comment edit), watch CI fire all v0.9.0 Phase 7 jobs in parallel, confirm green; download the `dep-graph-svg` and `fast-bench-results` artifacts. Close the PR without merging. |
| 10.N.X | v0.9.0 Phase 7 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.7 | NI | P3 | Phase 7 deviated from the plan's "Atomic commits per artifact" guidance for the same reason Phases 2 through 6 did: the user explicitly requested a single commit + push to main covering all six Phase 7 artifacts (action upgrades, functions coverage, check-prompts, CodeQL, fast-bench, dep-graph SVG). The six artifacts touch overlapping files (every one of them edits `.github/workflows/ci.yml`) so a true "one commit per artifact" split would generate noisy interim states. | Future phases that touch independent files should keep the atomic-commit pattern when the user does not explicitly request a single commit. Phase 7's overlap on `ci.yml` would make atomic-per-artifact noisier than the bundled commit; no follow-up needed. |
| 10.N.Y | v0.9.0 Phase 8 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 8.7 | NI | P3 | Phase 8 was instructed by sub-task 8.7 to ship the cycle-close PR via `/ship-and-babysit` (the autonomous slash command introduced in Phase 3.3). The user's invocation explicitly overrode that to "commit the generated commit message and push to main", so Phase 8 commits the cycle-close artifacts directly to `main` without an intermediate PR loop. The Phase 7 `coverage-diff.yml` / `pr-quality.yml` / `check-prompts` / `fast-bench` / `codeql.yml` jobs still fire on the push (post-merge analyse on `main` plus the weekly cron cover the CodeQL surface; the others run on every push regardless of PR state). | No follow-up needed. The push-to-main approach skips the `/ship-and-babysit` autonomous-CI-polling loop but does not bypass any gate: every Phase 7 CI workflow still fires on the push and the `tests/unit/workflow-discipline.test.ts` SHA-pinning gate is exercised on push. If the v0.10.0 cycle author opts for the PR route, the existing `/ship-and-babysit` skill at `.claude/commands/ship-and-babysit.md` covers it end-to-end. |
| 10.N.Z | v0.9.0 Phase 8 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 8.6 | NI | P3 | Phase 8.6 (optional v0.8.0 cycle CHANGELOG narrative at `docs/v0.8.0/CHANGELOG-CYCLE.md`) was explicitly skipped at v0.9.0 cycle close. The per-phase v0.8.0 history files under `docs/v0.8.0/development/history/phase-0N.md` plus the v0.8.0 Resolved table at `docs/v0.8.0/known-gaps.md` Section 10.2 already preserve the same content phase-by-phase, and `semantic-release` continues to author the per-commit `CHANGELOG.md`. Mirrors the v0.8.0 10.O.EE rationale (not a release blocker). | Optional v0.10.0 candidate: if a single consolidated cycle narrative is desired beyond the per-commit `CHANGELOG.md` and the per-phase history files, hand-author it outside the semantic-release flow at `docs/v0.8.0/CHANGELOG-CYCLE.md`. No release impact. |

### 10.2 Resolved

| ID | Source phase | Plan reference | Category | Resolved in | Notes |
|---|---|---|---|---|---|
| 10.O.D (v0.8.0) | v0.8.0 Phase 1 | (discovered) | BG | v0.9.0 Phase 1.1 | Vitest harness fix. Root cause traced to the `#!/usr/bin/env node` shebang on `bin/gemma-check.mjs` + `scripts/package-skills.mjs`; Vite's transform pipeline did not strip the leading `#!` line when those scripts were imported as ESM dependencies of a test, and Node's vm parser on Windows rejected the resulting source. Fix: 12-line `stripShebang` Vite plugin in `configs/vitest.config.ts` + `vitest` and `@vitest/coverage-v8` bumped from `^1.0.0` to `^2.1.9`. See v0.8.0/known-gaps.md row for the full notes. |
| 10.O.E (v0.8.0) | v0.8.0 Phase 1 | (discovered) | BG | v0.9.0 Phase 1.2 | Consolidator stress threshold raised from `<5000` ms to `<15000` ms (~36% headroom over the v0.8.0 measurement; vitest 2.x re-measurement on the same workstation is ~1.4s). Comment on the assertion cites the rationale and ADR-0002 / ADR-0018. |
| 10.O.G (v0.8.0) | v0.8.0 Phase 2 | docs/v0.8.0/plans/v0.8.0-cycle.md sub-task 2.8 | MT | v0.9.0 Phase 1.1 | SkillLoader round-trip tests in `tests/unit/scripts/package-skills.test.ts` now load and pass alongside the shebang fix. `scripts/package-skills.mjs.parseSkill` made CRLF-tolerant so Windows `core.autocrlf=true` working trees round-trip cleanly. |
| 10.O.N (v0.8.0) | v0.8.0 Phase 4 | (discovered) | BG | v0.9.0 Phase 1.1 | Full vitest run on Windows no longer segfaults after `MemoryStore.migration.test.ts` teardown. 218 files, 2464 tests, 0 failed. |
| 10.O.R (v0.8.0) | v0.8.0 Phase 5 | docs/v0.8.0/plans/v0.8.0-cycle.md sub-task 5.9 | MT | v0.9.0 Phase 1.1 | `tests/unit/cli/gemma-check.test.ts` (62 tests) now loads and passes end-to-end against `bin/gemma-check.mjs`, restoring the full spawn-level coverage that the scan-level sibling at `tests/unit/lib/checks-prompt-rules.test.ts` complements. |
| 10.O.K (v0.8.0) | v0.8.0 Phase 4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.1 | DF | v0.9.0 Phase 2.1 | `Gemma4StreamScrubber` wired into `StreamingPipeline._attemptStream` between the memory-context scrubber and the new `ToolCallStreamParser`. `ConversationManager.replayForCompaction()` strips `<think>` blocks from assistant messages before the compaction pipeline runs; `ContextCompactor.compact` consumes the replay view. Final assistant message persists only `parseChannel(...).visible`. |
| 10.O.M (v0.8.0) | v0.8.0 Phase 4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.2 | DF | v0.9.0 Phase 2.2 | `MemoryStore.retrieveHybrid` packs hybrid-ranked results with reason arrays into a budget-bounded Recalled Memories block. `UnifiedMemoryRetriever` routes the semantic layer through `retrieveHybrid` when the new `gemma-code.memory.scoringDefault` setting is `hybrid` (default); `legacy` preserves the v0.7.0 path for one cycle. |
| 10.O.S (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.3 | DF | v0.9.0 Phase 2.3 | `ContextCompactor.setRebuildSnapshotProvider(provider)` lets the rebuild branch rehydrate from a durable snapshot. On a non-empty snapshot it replaces conversation messages, emits an affordance, and returns `state: "ok"`; on null/throw it surfaces an improved `rebuild-needed` reason. Production wiring against `ChatHistoryStore.getSession` is staged for Phase 6 alongside the snapshot-restore UX. |
| 10.O.T (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.4 | DF | v0.9.0 Phase 2.4 | `MemoryPanel` subscribes to `vscode.window.onDidChangeActiveTextEditor` with 250ms debounce and calls `IntuitionCache.prefetch({currentFile, recentTools})` when the cache is wired and enabled. Snapshot payload exposes the prefetched entries as `anticipated`. New `gemma-code.memory.anticipatoryCache` setting (default `false`). Webview rendering of the section tracked under 10.N.C. |
| 10.O.U (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.5 | DF | v0.9.0 Phase 2.5 | New `reflect-worker` `SubAgentType` + `runReflectWorker(job, options)` worker dispatch. Cadence-gated (default 24h with injectable cursor) and hardware-tier-gated (`balanced`/`full` only). New `gemma-code.workers.reflect.enabled` setting. Production wiring of an idle-time scheduler (the v0.8.0 10.O.P carryover) is the natural place to advance the cadence cursor automatically. |
| 10.O.V (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.6 | DF | v0.9.0 Phase 2.6 | New `listProposedSkills(skillsRoot)` + `MemoryPanel` Inspect / Accept / Dismiss handlers for `<root>/proposed/<slug>/SKILL.md` drafts. Accept copies the draft to `<root>/<slug>/SKILL.md` and removes the proposed copy; Dismiss deletes the proposed file. Webview rendering tracked under 10.N.C. |
| 10.O.W (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.7 | DF | v0.9.0 Phase 2.7 | `LLMChatRequest.keep_alive?: number | string` added; `StreamingPipeline` accepts an optional `KeepAliveResolver` callback that merges the registry hint into each streamed chat request. Production wiring of `ModelPinRegistry` into `ChatPanelBootstrap` tracked under 10.N.A. |
| 10.O.Y (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.8 | DF | v0.9.0 Phase 2.8 | `ChatHistoryStore` schema v2 introduces `tool_call_bytes(session_id, call_id, bytes, ts)` with cascade-on-session-delete. `ConversationManager.storeToolCallBytes` writes through to the persistent store; `getToolCallBytes` falls back to it on in-memory LRU miss. |
| 10.O.Z (v0.8.0) | v0.8.0 Phase 6 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 2.9 | DF | v0.9.0 Phase 2.9 | `ToolCallStreamParser` driven from `StreamingPipeline._attemptStream`; emitted events forward to the webview verbatim. `SubAgentStatusMessage.agentType` extended for `reflect-worker`. Progressive card rendering tracked under 10.N.B. |
| 10.O.P (v0.8.0) | v0.8.0 Phase 5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.1 | DF | v0.9.0 Phase 6.1 | `IdleTimeScheduler` pure module landed in [src/agents/IdleTimeScheduler.ts](../../src/agents/IdleTimeScheduler.ts) with injectable clock + timers + activity source. Curator-recommended thresholds: `idleThreshold: 5 min`, `cadence: 12h`. Reflect-recommended thresholds: `idleThreshold: 10 min`, `cadence: 24h`. Production wiring (panel bootstrap registration) tracked under 10.N.Q. Unit tests cover idle / cadence / activity-reset / multi-task gating in `tests/unit/agents/IdleTimeScheduler.test.ts`. |
| 10.O.F (v0.8.0) | v0.8.0 Phase 2 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.2 | NI | v0.9.0 Phase 6.2 | `AgentLoop.creditSubAgentVerification(result)` credits the parent pass-state gate when a verification / audit-worker / testgaps-worker / curator-worker sub-agent returns `{success: true}`. Wired into the four in-loop dispatch sites. New `subAgentVerificationCredit` option (default `true`); new `gemma-code.passStateGating.subAgentCredit` setting (default `true`). Failed sub-agents and `reflect-worker` returns do NOT credit. Tests added to `tests/unit/tools/AgentLoop.test.ts` cover positive credit, opt-out, failure no-credit, and type filter. |
| 10.O.L (v0.8.0) | v0.8.0 Phase 4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.3 | DF | v0.9.0 Phase 6.3 | `/thinking-mode <preset>` now emits a one-line `[Thinking mode: <preset>]` chat affordance after the setting update. An already-in-flight stream finishes with the prior preset; the next streaming request picks up the new preset via the panel's existing settings-change listener. New unit test in `tests/unit/panels/ChatCommandHandlers.test.ts`. |
| 10.O.I (v0.8.0) | v0.8.0 Phase 3 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.4 | WN | v0.9.0 Phase 6.4 | `PlanArchive.computeDiff` clean mode now strips trailing newlines from add/del runs before wrapping with `**` / `~~` and re-emits the newlines AFTER the closing marker. New `wrapDiffRun` helper exported for direct test coverage. Updated existing diff test + added two regression tests asserting `**...\n**` patterns no longer appear and that pure-newline / multi-newline / strikethrough runs are handled. |
| 10.O.H (v0.8.0) | v0.8.0 Phase 3 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.5 | NI | v0.9.0 Phase 6.5 | `loadHook` (and therefore `renderHookAsSystemMessage`) now runs the v0.8.0 Phase 2.7 `PromptInjectionScanner` against the hook body. Matching hooks are dropped with a logged warning. New `LoadHookOptions.scanInjection` (default `true`); new `gemma-code.hooks.scanInjection` setting (default `true`). Wired through `ChatMessageRouter._handlePlanToggle`. Five new tests cover injection-pattern drop, scan opt-out, benign pass-through, invisible-unicode drop, and `<system>` tag drop. |
| 10.O.Q (v0.8.0) | v0.8.0 Phase 5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.6 | DF | v0.9.0 Phase 6.6 | `ToolRegistry.registerLazy(name, factory)` defers handler instantiation until the first `execute()` call. `buildToolRegistry` wires tier `confirm` / `dangerous` tools (write / edit / create / delete / run_terminal / web_search / fetch_page) via `registerLazy` and keeps tier `auto-approve` tools (read_file / list_directory / grep_codebase + the optional compress_* + update_todos) eager. New `listLazyToolNames` / `listEagerToolNames` exports drive the integration test in `tests/integration/tools/lazy-import-builder.test.ts`. Boot-time savings limited by transitive imports tracked under 10.N.R. |
| 10.O.J (v0.8.0) | v0.8.0 Phase 4 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.7 | DF | v0.9.0 Phase 6.7 | `tests/integration/llm/LmStudioClient.live.test.ts` exercises one streamed completion against a real LM Studio server on `127.0.0.1:1234`. Skipped by default via `describe.runIf(process.env.LMSTUDIO_LIVE === "1")`; opt-in via `LMSTUDIO_LIVE=1` (URL override via `LMSTUDIO_BASE_URL`). Documented in `CONTRIBUTING.md` Testing section. |
| 10.O.O (v0.8.0) | v0.8.0 Phase 5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 6.8 | WN | v0.9.0 Phase 6.8 | `src/skills/catalog/{harden,distill,build-second-brain,animate}/SKILL.md` all trimmed under the 800-token budget (`gemma-check --rule prompt-oversized` returns 0 findings on the four). For `build-second-brain` the verbose interview script + extraction examples were extracted to a sibling `examples.md` referenced from the SKILL.md. `docs/index.md` regenerated. The lone outstanding `prompt-oversized` warning (`review-pr/SKILL.md ~811 tokens`) remains tracked under 10.N.F. |
| 10.O.AB (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.1 | DF | v0.9.0 Phase 7.1 | GitHub Actions across `.github/workflows/*.yml` bumped to v5 line (or v6 for setup-python), SHA-pinned with version comments: `actions/checkout@v5.0.1`, `actions/setup-node@v5.0.0`, `actions/setup-python@v6.2.0`, `actions/upload-artifact@v5.0.0`, `actions/download-artifact@v5.0.0`, `actions/cache@v5.0.5`. TypeScript test/build/lint matrix in `ci.yml` now `["22.x", "24.x"]` -- Node 20 dropped, all standalone `node-version: "20"` references upgraded to `"22"`. `tests/unit/workflow-discipline.test.ts` SHA-pinning gate stays green. Beats GitHub's 2026-09-16 Node 20 deadline. |
| 10.O.AC (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.2 | MT | v0.9.0 Phase 7.2 | `coverage-gate` job in `.github/workflows/ci.yml` now also asserts `functions >= 80` (third awk check reading `.total.functions.pct` from `coverage/coverage-summary.json`). `configs/vitest.config.ts` thresholds extended with `functions: 80`. Global function coverage 88.94% clears the gate; the 31 per-file low-coverage files stay tracked under 10.N.T. |
| 10.O.AD (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.3 | DF | v0.9.0 Phase 7.3 | New `check-prompts` job in `.github/workflows/ci.yml` runs `npm run check:prompts` on every push/PR (prompt-no-ascii-violation / prompt-oversized / prompt-trailing-whitespace / prompt-bom / skill-duplicate-name rules against `src/chat/prompts` + `src/skills/catalog`). Fails workflow only on `severity: error` findings (warnings non-blocking per the v0.8.0 Phase 7 CLI realignment). The outstanding `review-pr/SKILL.md ~811-token` warning stays tracked under 10.N.F. |
| 10.O.AE (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.4 | DF | v0.9.0 Phase 7.4 | New `.github/workflows/codeql.yml` workflow runs `github/codeql-action@v3.35.5` `init` + `analyze` (SHA-pinned) against `javascript-typescript` with the `security-and-quality` query pack. Triggers: push to `main`, PR to `main`, weekly cron (`27 4 * * 1`). Starts non-blocking (`continue-on-error: true` on analyze) per the plan; SARIF uploads to the GitHub Security tab. Flip-to-blocking is operator-driven and tracked under 10.N.U. |
| 10.O.AF (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.5 | DF | v0.9.0 Phase 7.5 | New `fast-bench` job in `.github/workflows/ci.yml` runs only rendering benches (`-t render`) on every push, uses the existing JSON reporter at `scripts/vitest-bench-json-reporter.mjs`, and drives `scripts/check-bench-regressions.mjs` against `tests/benchmarks/baselines/v0.7.0.json` with `--regression-pct 20 --fail-on-regression`. The script gains an explicit `--fail-on-regression` flag (forward-compatible alias; default exit code unchanged). Bench results uploaded as 14-day artifact. Nightly bench unchanged. The v0.8.0 baseline migration stays tracked under 10.N.V. |
| 10.O.AG (v0.8.0) | v0.8.0 Phase 7 post-CI | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.6 | DF | v0.9.0 Phase 7.6 | `check-architecture` job in `.github/workflows/ci.yml` now installs Graphviz (`apt-get install -y --no-install-recommends graphviz`), regenerates `docs/v0.5.0/dep-graph.svg` via `npm run deps:graph`, and uploads it as the `dep-graph-svg` artifact with 7-day retention. Reviewers can download the post-refactor dep graph directly from the workflow summary. |
| 10.N.G | v0.9.0 Phase 3.2 / 3.3 / 3.5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 8.1 | NI | v0.9.0 Phase 8.1 | Closed. [AGENTS.md](../../AGENTS.md) header sentence rewritten to explicitly carve out `.claude/agents/` and `.claude/commands/` as repo-tracked agent-agnostic Markdown artifacts (the rest of `.claude/` remains personal-only and gitignored). New "Claude Code addenda (v0.9.0)" section at the bottom of AGENTS.md lists the four files (`pr-manager.md`, `pr-manager-lite.md`, `taskmaster.md`, `ship-and-babysit.md`) with one-line descriptions; reaffirms the tool-agnostic invariant; cross-links CONTRIBUTING-BEGINNERS.md. README.md also updated to list the four files in the PR-lifecycle section. |
| 10.O.EE (v0.8.0) | v0.8.0 Phase 7 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 8.6 | DF | v0.9.0 Phase 8.6 (deferred) | Closed-as-explicitly-deferred. The optional `docs/v0.8.0/CHANGELOG-CYCLE.md` cycle-narrative artifact is NOT shipped in v0.9.0; rationale documented in v0.8.0/known-gaps.md 10.O.EE transferred footer and in 10.N.Z above. The per-phase history files at `docs/v0.8.0/development/history/phase-0N.md` plus the v0.8.0 Resolved table at `docs/v0.8.0/known-gaps.md` 10.2 already preserve the same content, and `semantic-release` continues to author the per-commit `CHANGELOG.md`. Not a release blocker. |
| 10.N.V | v0.9.0 Phase 7.5 | docs/v0.9.0/plans/v0.9.0-cycle.md sub-task 7.5 | DF | v0.9.0 post-Phase-8 (2026-05-17 nightly hotfix) | Closed. `tests/benchmarks/baselines/v0.9.0.json` captured on the post-Phase-0.9 (cached `Marked` instance) renderer + post-Phase-7 codebase via `npm run bench` + `scripts/check-bench-regressions.mjs --update-baseline`. The nightly gate `.github/workflows/nightly.yml` now compares against `v0.9.0.json` with `v0.6.0.json` as the long-arc floor; the `fast-bench` PR-time gate in `.github/workflows/ci.yml` also bumped to v0.9.0. Closes the false-positive surfaced by the 2026-05-17 nightly run (render benches off ~50% vs v0.6.0's `marked@v4`-era capture; wtinylfu off 28% from runner variance with no code change since v0.6.0). Workstation was active not strictly quiescent during capture; two benches retain high rme (`render ~2000-token message` 36%, `check-tool-permission` 39%) which the 20% threshold absorbs for the other 26 entries -- a future quiescent re-capture under v0.8.0 10.O.BB Section 1 will tighten those two. |

### 10.3 Summary (v0.9.0 in-cycle, post-Phase-8 hotfix)

| Category | Open | Resolved |
|---|---|---|
| NI (not implemented) | 7 | 4 |
| DF (deferred) | 15 | 19 |
| BG (bug) | 0 | 3 |
| MT (missing tests) | 2 | 3 |
| WN (warning) | 0 | 2 |
| QG (gate bypass) | 0 | 0 |
| **Total** | **24** | **31** |

**Status (Phase 8 close, 2026-05-16)**: Cycle artifacts shipped per Phase 8 sub-tasks: (1) [README.md](../../README.md) gains a v0.9.0 Highlights section at the top (37-item closure, reverse-engineered dev-loop tooling, skill-native artifacts, CI hardening), a Bounded-output test runners subsection (`npm run debug ...`), a Working with issues subsection (`npm run work` + `npm run deep-work` + `npm run agent-batch`), a PR lifecycle subsection (side-by-side `npm run review` + `.claude/commands/ship-and-babysit.md` with the overlap-and-fold note from 10.N.O), and a Contributing first-PR cross-link to [CONTRIBUTING-BEGINNERS.md](../../CONTRIBUTING-BEGINNERS.md). (2) [AGENTS.md](../../AGENTS.md) header sentence rewritten to carve out `.claude/agents/` + `.claude/commands/` from the personal-only `.claude/` policy (closes 10.N.G); new "Claude Code addenda (v0.9.0)" section at the bottom lists the four committed Markdown artifacts (`pr-manager.md`, `pr-manager-lite.md`, `taskmaster.md`, `ship-and-babysit.md`) with one-line descriptions and reaffirms the tool-agnostic invariant; cross-link to CONTRIBUTING-BEGINNERS.md added. (3) `npm run catalog` regenerated [docs/index.md](../index.md); both `npm run catalog:check` and `npm run perm-tier:check` exit 0. (4) [docs/v0.8.0/known-gaps.md](../v0.8.0/known-gaps.md) `Status:` flipped from `transferred-to-v0.9.0` to `finalized`; each of the 9 still-open rows (10.O.A / B / C / X / AA / BB / CC / DD / EE) gains an inline `(transferred to docs/v0.9.0/plans/v0.9.0-cycle.md sub-task <N.M>)` footer pointing at either sub-task 1.3 (operator-actions) or 8.6 (deferred); the Resolved-vs-Transferred Summary table is recomputed (42 resolved, 9 transferred, 0 open). (5) DEVLOG cycle-narrative entry added at [docs/DEVLOG.md](../DEVLOG.md). (6) Phase 8.6 explicitly deferred per 10.O.EE / 10.N.Z (per-phase v0.8.0 history files plus the v0.8.0 Resolved table already carry the same content; semantic-release authors the per-commit CHANGELOG). (7) The cycle-close artifacts ship as a single commit pushed directly to `main` per the user's invocation override of sub-task 8.7 -- two new in-cycle items (10.N.Y atomic-commit deviation, 10.N.Z 8.6 deferral) track that decision. v0.10.0 inherits 25 open items (24 in-cycle deferrals carried over from Phases 1-7 plus the new 10.N.Y / 10.N.Z) and the 9 operator-only items in [operator-actions.md](operator-actions.md).

**Status (Phase 7 close, 2026-05-16)**: Six Phase 7 sub-tasks shipped, closing 6 v0.8.0 post-CI audit follow-ups (10.O.AB / AC / AD / AE / AF / AG) across the `.github/workflows/` tree: (1) Every `actions/*` reference bumped to v5 line (or v6 for setup-python) and SHA-pinned across `ci.yml` / `nightly.yml` / `golden-tasks.yml` / `release.yml` / `installer-smoke.yml` / `commitlint.yml` / `coverage-diff.yml` / `pr-quality.yml` / `branch-cleanup.yml` / `semantic-release.yml`. TypeScript matrix in `ci.yml` moved to `["22.x", "24.x"]`; standalone `node-version: "20"` references upgraded to `"22"`. Beats GitHub's 2026-09-16 Node 20 deadline. (2) `coverage-gate` extended with `functions >= 80` (third awk check). `configs/vitest.config.ts` thresholds extended likewise. Global functions coverage 88.94% (1199 / 1348) clears the gate. (3) New `check-prompts` job runs `npm run check:prompts` on every push/PR; fails workflow only on error-severity prompt-rule findings (warnings non-blocking per v0.8.0 Phase 7 CLI realignment). (4) New `.github/workflows/codeql.yml` workflow runs `github/codeql-action@v3.35.5` with `security-and-quality` query pack on `javascript-typescript`. Triggers on push/PR to `main` plus a weekly cron. Starts non-blocking (`continue-on-error: true`) per plan; flip to blocking after one clean week tracked under 10.N.U. (5) New `fast-bench` job runs `-t render` only on every push, uses `scripts/vitest-bench-json-reporter.mjs`, and drives `scripts/check-bench-regressions.mjs` against `tests/benchmarks/baselines/v0.7.0.json` with the new `--fail-on-regression` flag. v0.8.0-baseline migration tracked under 10.N.V. (6) `check-architecture` job now installs Graphviz, runs `npm run deps:graph`, and uploads `dep-graph-svg` as a 7-day artifact. Full Windows suite green: 227 files, 2636 tests passed + 5 skipped, 0 failed. `npm run lint`, `npm run build`, `npm run check src/`, `npm run check:prompts` (1 pre-existing warning, no errors), `npm run deps:check`, `npm run catalog:check`, `npm run perm-tier:check` all exit 0. Five new in-cycle deferrals (10.N.T per-file function-coverage backfill, 10.N.U CodeQL flip-to-blocking decision, 10.N.V v0.8.0-baseline migration, 10.N.W live-PR smoke acceptance, 10.N.X Phase 7 atomic-commit deviation).

**Status (Phase 6 close, 2026-05-16)**: Eight Phase 6 sub-tasks shipped, closing 8 v0.8.0 in-cycle carryovers (10.O.F / H / I / J / L / O / P / Q): (1) `src/agents/IdleTimeScheduler.ts` is a new pure module with injectable clock + timers + activity source, exposes `register` / `unregister` / `notifyActivity` / `tick` / `start` / `stop`, and is exercised by 10 unit tests covering idle / cadence / activity-reset / multi-task gating. Curator-recommended thresholds (5 min idle / 12 h cadence) and reflect-recommended thresholds (10 min idle / 24 h cadence) are documented; production wiring into `ChatPanelBootstrap` deferred under 10.N.Q. (2) `AgentLoop.creditSubAgentVerification(result)` credits the parent pass-state gate when verification / audit-worker / testgaps-worker / curator-worker returns success; wired into the four dispatch sites in `_runOneIteration`. New `subAgentVerificationCredit` option (default `true`) and `gemma-code.passStateGating.subAgentCredit` setting; failed sub-agents and `reflect-worker` do NOT credit. Four new tests in `tests/unit/tools/AgentLoop.test.ts`. (3) `/thinking-mode <preset>` emits `_[Thinking mode: <preset>] Sampler preset applies to the next streaming request._` chat affordance via existing `_emitMarkdown` -> `messageComplete` path; in-flight stream keeps prior preset (deliberate per v0.8.0 design). New unit test asserts the affordance text. (4) `PlanArchive.computeDiff` clean mode strips trailing newlines before wrapping `**` / `~~` and re-emits newlines AFTER the closing marker; new `wrapDiffRun` helper exported for direct test coverage; 2 new regression tests on `tests/unit/storage/PlanArchive.test.ts`. (5) `loadHook` runs the v0.8.0 Phase 2.7 `PromptInjectionScanner` against the hook body and drops matching content with a logged warning; new `LoadHookOptions.scanInjection` (default `true`) and `gemma-code.hooks.scanInjection` setting; wired through `ChatMessageRouter._handlePlanToggle`. Five new tests in `tests/unit/chat/ImprovementHook.test.ts` cover injection-pattern drop, scan opt-out, benign pass-through, invisible-unicode drop, `<system>` tag drop, and the renderHook forwarding. (6) `ToolRegistry.registerLazy(name, factory)` defers handler instantiation until the first `execute()`; `buildToolRegistry` wires tier `confirm` / `dangerous` tools (write / edit / create / delete / run_terminal / web_search / fetch_page) via `registerLazy`; tier `auto-approve` tools stay eager. New `listLazyToolNames` / `listEagerToolNames` exports drive the integration test at `tests/integration/tools/lazy-import-builder.test.ts`. 6 new unit tests in `tests/unit/tools/ToolRegistry.test.ts` cover the lazy mechanism (has / isEnabled / getEnabledNames visible before factory runs, factory runs once + caches, resolveLazy semantics, async factory support, eager-overrides-lazy). Boot-time savings limited by transitive imports through `PermissionTiers` / `ActionClassifier` / `McpClient` / `SubAgentManager` tracked under 10.N.R. (7) `tests/integration/llm/LmStudioClient.live.test.ts` exercises a real LM Studio streaming round-trip behind `describe.runIf(process.env.LMSTUDIO_LIVE === "1")`; documented in CONTRIBUTING.md Testing section; URL override via `LMSTUDIO_BASE_URL`. (8) Four oversized SKILL.md bodies (`harden`, `distill`, `build-second-brain`, `animate`) trimmed under the 800-token budget; for `build-second-brain` the verbose interview script + extraction examples were extracted to a sibling `examples.md`. `npm run check:prompts` returns 1 warning (`review-pr/SKILL.md ~811 tokens`, tracked under 10.N.F) -- the four Phase 6.8 targets are all clean. Full Windows suite green: 227 files, 2636 tests passed + 5 skipped (pre-existing), 0 failed. `npm run lint`, `npm run build`, `npm run check src/`, `npm run deps:check`, and `npm run perm-tier:check` all exit 0. `docs/index.md` regenerated. Three new in-cycle deferrals (10.N.Q IdleTimeScheduler production wiring, 10.N.R lazy-import transitive-imports cleanup, 10.N.S Phase 6 atomic-commit deviation).

**Status (Phase 5 close, 2026-05-16)**: Four Phase 5 cross-platform Node CLIs shipped, all reverse-engineered into vendor-neutral Gemma-Code-native form with no third-party PR-as-service or review-as-service calls: (1) `scripts/deep-work/{cli,shared,start,pick,continue,status,cleanup}.mjs` wired as `npm run deep-work` provides the full `start` / `pick` / `continue` / `status` / `list` / `cleanup` lifecycle -- `start` creates a worktree at `worktrees/issue-<num>-<slug>` on branch `feat/issue-<num>-<slug>`, prints the agent prompt, and copies it to the clipboard; `pick` lists `good first issue` candidates with `--first` auto-dispatch; `status` reads `git worktree list --porcelain` and tabulates branch / HEAD / dirty; `cleanup` refuses on dirty without `--force` and refuses non-interactive without `--yes`. `worktrees/` added to `.gitignore`. (2) `.github/PULL_REQUEST_TEMPLATE.md` + `scripts/check-pr-checklist.mjs` + `.github/workflows/pr-quality.yml` enforce the Submission Checklist: every `- [ ]` box must be either ticked (`- [x]`) or explicitly opted out as `- [ ] N/A: <reason>`; the workflow's `gh pr view --json body --jq .body` injection avoids logging the body and re-runs `node scripts/check-pr-checklist.mjs` against it. (3) `scripts/agent-batch/{cli,schema,validate,overlap,launch,status}.mjs` wired as `npm run agent-batch` with Zod-validated specs (`batchId` + tasks of `{issue, agent: claude|codex|cursor, extraPrompt?, dependsOn?}`); `validate` runs the schema; `overlap` detects duplicate issues, missing deps, and dependency cycles (DFS visited-on-stack); `launch` topo-sorts and dry-runs the dispatch table by default (only `--apply` actually creates worktrees via the deep-work `startCommand`); `status` classifies each task pending/running/done from local worktree commit state alone. Sample spec at `examples/agent-batch.spec.json`. (4) `scripts/review/{cli,shared,sync,review,fix,coverage,merge}.mjs` wired as `npm run review` -- the imperative cousin of the autonomous `/ship-and-babysit` slash command (overlap documented in both CLI headers, with a v0.10.0 fold-one-if-usage-converges note): `sync` refuses on dirty tree and does `gh pr checkout` + fetch + merge `origin/main`; `review` invokes the Phase 3.1 review-pr SKILL via the configured agent CLI (or prints the prompt); `fix` fetches reviewer comments (`gh api pulls/<n>/comments` + `gh pr view --json comments`) and hands off to the `.claude/agents/pr-manager` subagent; `coverage` downloads the `diff-coverage` artifact from the `Coverage Diff` workflow and prints + suggests test files for uncovered lines; `merge` refuses if `gh pr checks` shows red/pending and defaults to `--squash`. Four test files added: `tests/integration/scripts-deep-work.test.ts` (19 tests; shared helpers, formatters, cli main, spawn smoke), `tests/unit/scripts/check-pr-checklist.test.ts` (7 tests; fully-checked / one-unchecked / N/A-reason / no-section / empty-section / extractor / multi-section), `tests/unit/scripts/agent-batch.test.ts` (21 tests; schema acceptance + rejection, duplicate / missing-deps / cycle detection, topological order, dispatch + status formatters, cli --help / unknown / validate-against-disk), and `tests/integration/scripts-review.test.ts` (20 tests; parseReviewArgs / parseGhPrChecks / checksAreGreen / extractUncoveredFromMarkdown / suggestTestFiles / formatCoverageReport / summarizeReviewerComments / cli --help / unknown / sync-dryrun / merge-dryrun / spawn smoke). Full Windows suite green: 225 files, 2603 tests passed + 4 skipped (pre-existing), 0 failed. `npm run lint`, `npm run build`, `npm run check src/`, `npm run deps:check`, `npm run catalog:check`, and `npm run perm-tier:check` all exit 0. Five new in-cycle deferrals (10.N.L deep-work live-issue smoke, 10.N.M pr-quality real-PR smoke, 10.N.N agent-batch live `--apply` dispatch, 10.N.O review CLI live `sync` / `merge` smoke + v0.10.0 fold decision, 10.N.P Phase 5 atomic-commit deviation).

**Status (Phase 4 close, 2026-05-16)**: Four dev-loop ergonomics artifacts shipped, all cross-platform Node (no bash dependencies on Windows), all reverse-engineered from public OpenHuman bash equivalents into vendor-neutral Gemma-Code-native form: (1) `scripts/debug/cli.mjs` + five thin sibling delegates (`unit.mjs`, `integration.mjs`, `golden.mjs`, `bench.mjs`, `logs.mjs`) tee vitest stdout/stderr to `out/debug-logs/<kind>-<ISO-ts>.log`, append `# exit <code>` as the last log line, and expose `logs list` / `logs last` / `logs <run-id>` for after-the-fact inspection -- the runner is summary-by-default and switches to verbose pass-through with `--verbose`; (2) `.github/workflows/coverage-diff.yml` runs `diff-cover==9.2.0` (pinned) against `coverage/lcov.info` with `--fail-under=80 --compare-branch=origin/main`, uploads the markdown + HTML reports as a 14-day artifact, and `gh pr comment`s the markdown report back to the failing PR -- the global vitest thresholds in `configs/vitest.config.ts` are NOT modified (the gate is purely additive); (3) `.husky/pre-push` runs `eslint --fix` (best-effort), refuses the push with a re-stage / re-push prompt if the fix actually changed files, then re-runs strict `npm run lint`, `npm run build`, and `npm run check src/` without any `|| true` muting; (4) `scripts/work.mjs` (wired as `npm run work <issue>`) fetches a GitHub issue via `gh issue view`, derives `feat/issue-<num>-<slug>` (slug capped at 40 chars + trailing-dash trim), creates/switches the branch unless `--no-checkout`, prints the prompt and copies it to the clipboard (`clip` / `pbcopy` / `xclip`), and optionally spawns `claude` / `codex` / `cursor` with the prompt piped on stdin. Three test files added: `tests/unit/scripts/debug.test.ts` (14 tests; exercises `buildVitestArgs`, `summarizeVitestOutput`, `extractFailureBlocks`, `listLogs`, `main` --help / unknown / `logs list`, plus a spawn-level help smoke), `tests/unit/scripts/work.test.ts` (18 tests; `parseArgs`, `slugify`, `deriveBranchName`, `buildAgentPrompt` including label arrays and missing fields, plus `main` --help / no-arg), and `tests/integration/husky-prepush.test.ts` (7 tests; textual contract of the hook). `out/debug-logs/` added to `.gitignore`. Full Windows suite green: 222 files, 2536 tests passed + 4 skipped (pre-existing), 0 failed. `npm run lint`, `npm run build`, `npm run check src/`, `npm run deps:check`, `npm run catalog:check`, and `npm run perm-tier:check` all exit 0. Three new in-cycle deferrals (10.N.I diff-cover live-PR smoke, 10.N.J pre-push real-branch smoke, 10.N.K Phase 4 atomic-commit deviation). The two earlier webview-rendering items (10.N.B / 10.N.C) had their suggested next step migrated from Phase 4 to Phase 6 "UX polish" because Phase 4's scope was dev-loop only and did not touch the webview.

**Status (Phase 3 close, 2026-05-16)**: Five Phase-3 markdown artifacts shipped: enriched `src/skills/catalog/review-pr/SKILL.md` (v2.0.0, reverse-engineered walkthrough / per-file / severity-x-confidence / review-first-then-apply structure, gemma-check warning-free at ~800 tokens), `.claude/agents/pr-manager.md` + `.claude/agents/pr-manager-lite.md` (existing-reviewer-comment loop, plain `gh api` and GraphQL `resolveReviewThread`, no third-party review-as-service), `.claude/commands/ship-and-babysit.md` (commit / push / open PR / babysit Gemma-Code's own CI every ~270s, hard cap 12 ticks, explicit exclusion of CodeRabbit polling), `.claude/agents/taskmaster.md` (read-only inputs except `docs/todos.md`, never deletes a row, always cites the source SHA / # / ID), and `CONTRIBUTING-BEGINNERS.md` (~6.8 KB end-to-end first-PR walkthrough with CONTRIBUTING.md cross-link). `.gitignore` adjusted to allow `.claude/agents/` and `.claude/commands/` to be tracked while keeping personal Claude Code state (`hooks/`, `settings.local.json`, `scheduled_tasks.lock`) ignored. Full Windows suite green: 218 files, 2497 tests, 0 failed. `npm run lint`, `npm run build`, `npm run check`, `npm run check:prompts`, `npm run deps:check`, `npm run catalog:check`, and `npm run perm-tier:check` all exit 0. Three new deferrals (10.N.F / 10.N.G / 10.N.H) cover the SKILL token-budget tension, the AGENTS.md sentence about `.claude/` being personal-only, and the v0.10.0 candidate to extend the `prompt-*` rule globs to `.claude/agents` + `.claude/commands`. The pre-existing 10.N.B / 10.N.C webview-rendering items remain open and migrate from "Phase 3" to "Phase 4" suggested-next-step.

**Status (Phase 2 close, 2026-05-16)**: Nine v0.8.0 carryovers closed (10.O.K / M / S / T / U / V / W / Y / Z). Five new in-cycle deferrals (10.N.A through 10.N.E) cover the production composition-root wiring and the webview-side rendering that remain after the backend wiring landed. Full Windows test suite green: 218 files, 2497 tests, 0 failed. `npm run lint`, `npm run build`, `npm run deps:check`, `npm run catalog:check`, and `npm run perm-tier:check` all exit 0. The bench latency-regression check and Windows + live-Ollama manual smoke are operator-driven (tracked as 10.N.D).

**Status (Phase 1 close, 2026-05-16)**: Five carryovers from v0.8.0 closed (10.O.D / E / G / N / R). Full Windows test suite is now green: 218 files, 2464 tests, 0 failed, no segfault. `npm run lint`, `npm run check src/`, `npm run deps:check`, `npm run catalog:check`, and `npm run perm-tier:check` all exit 0. The eight operator-only carryovers are tracked separately in [operator-actions.md](operator-actions.md). Phase 1 introduced no new in-cycle gaps.

Phase 1 also opportunistically resolved two pre-existing Windows-autocrlf failures discovered after the segfault cleared: `tests/unit/scripts/check-architecture.test.ts` now tolerates CRLF when comparing the `.sh` shebang, and `scripts/generate-tool-permission-table.mjs` normalises CRLF -> LF before its self-sync check (the `perm-tier:check` gate was reporting "out of sync" against a content-identical doc whose line endings round-tripped through autocrlf). Both fixes are scoped to comparison points only; on-disk source files keep their committed LF semantics for POSIX consumers.
