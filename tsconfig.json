{
  "//": "v1.4.0 Phase 7 (T021 / gap 1.1.P1.A) -- the root project: src/ (the un-migrated coding sub-trees: activation, desktop, panels, storage, tools, extension.ts) plus modules/coding/**. It REFERENCES core (the composite shared-core project) rather than including it, so `tsc -b` builds core first and this project consumes core's emitted declarations -- no double-emit of core into out/. Shared compiler options live in tsconfig.base.json. The desktop/ workspace is intentionally outside this graph: it bundles core via esbuild (sidecar) / Vite (UI) and type-checks with its own `tsc --noEmit`, so it does not consume tsc project-reference output.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./out",
    "rootDirs": ["./src", "./core", "./modules"],
    "declaration": false,
    "declarationMap": false
  },
  "references": [{ "path": "./core" }],
  "include": ["src/**/*", "modules/**/*"],
  "exclude": ["node_modules", "out", "tests", "modules/**/*.test.ts"]
}
